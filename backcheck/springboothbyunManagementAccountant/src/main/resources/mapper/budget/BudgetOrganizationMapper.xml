<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.management.accountant.oracle.mapper.budget.BudgetOrganizationMapper">

    <select id="selectByOrganizationCode" resultType="com.management.accountant.oracle.entity.budget.BudgetOrganization">
        SELECT * FROM TBL_BUDGET_ORGANIZATION WHERE ORGANIZATION_CODE = #{organizationCode} AND DEL_FLAG = 0
    </select>

    <select id="selectByOrganizationType" resultType="com.management.accountant.oracle.entity.budget.BudgetOrganization">
        SELECT * FROM TBL_BUDGET_ORGANIZATION WHERE ORGANIZATION_TYPE = #{organizationType} AND DEL_FLAG = 0 ORDER BY SORT_ORDER
    </select>

    <select id="selectByParentId" resultType="com.management.accountant.oracle.entity.budget.BudgetOrganization">
        SELECT * FROM TBL_BUDGET_ORGANIZATION WHERE PARENT_ID = #{parentId} AND DEL_FLAG = 0 ORDER BY SORT_ORDER
    </select>

    <select id="selectOrganizationTree" resultType="com.management.accountant.oracle.entity.budget.BudgetOrganization">
        SELECT * FROM TBL_BUDGET_ORGANIZATION
        WHERE DEL_FLAG = 0
        <if test="parentId != null and parentId != ''">
            AND PARENT_ID = #{parentId}
        </if>
        ORDER BY ORGANIZATION_LEVEL, SORT_ORDER
    </select>

    <select id="selectEnabledOrganizations" resultType="com.management.accountant.oracle.entity.budget.BudgetOrganization">
        SELECT * FROM TBL_BUDGET_ORGANIZATION WHERE IS_ENABLED = 1 AND DEL_FLAG = 0 ORDER BY SORT_ORDER
    </select>

    <update id="batchDeleteOrganizations">
        UPDATE TBL_BUDGET_ORGANIZATION SET DEL_FLAG = 1 WHERE ORGANIZATION_ID IN
        <foreach collection="organizationIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>

