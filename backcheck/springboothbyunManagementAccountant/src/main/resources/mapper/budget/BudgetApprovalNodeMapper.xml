<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.management.accountant.oracle.mapper.budget.BudgetApprovalNodeMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.management.accountant.oracle.entity.budget.BudgetApprovalNode">
        <id column="NODE_ID" property="nodeId"/>
        <result column="FLOW_ID" property="flowId"/>
        <result column="NODE_NAME" property="nodeName"/>
        <result column="NODE_CODE" property="nodeCode"/>
        <result column="NODE_TYPE" property="nodeType"/>
        <result column="APPROVERS" property="approvers"/>
        <result column="APPROVAL_MODE" property="approvalMode"/>
        <result column="STATUS" property="status"/>
        <result column="APPROVAL_RESULT" property="approvalResult"/>
        <result column="APPROVAL_COMMENT" property="approvalComment"/>
        <result column="APPROVAL_TIME" property="approvalTime"/>
        <result column="ORDER_NUM" property="orderNum"/>
        <result column="NODE_CONFIG" property="nodeConfig"/>
        <result column="TIMEOUT_HOURS" property="timeoutHours"/>
        <result column="DESCRIPTION" property="description"/>
        <result column="CREATE_TIME" property="createTime"/>
        <result column="UPDATE_TIME" property="updateTime"/>
        <result column="CREATOR_ID" property="creatorId"/>
        <result column="CREATOR_NAME" property="creatorName"/>
        <result column="UPDATER_ID" property="updaterId"/>
        <result column="UPDATER_NAME" property="updaterName"/>
        <result column="COMPANY_ID" property="companyId"/>
        <result column="COMPANY_NAME" property="companyName"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        NODE_ID, FLOW_ID, NODE_NAME, NODE_CODE, NODE_TYPE, APPROVERS,
        APPROVAL_MODE, STATUS, APPROVAL_RESULT, APPROVAL_COMMENT, APPROVAL_TIME,
        ORDER_NUM, NODE_CONFIG, TIMEOUT_HOURS, DESCRIPTION,
        CREATE_TIME, UPDATE_TIME, CREATOR_ID, CREATOR_NAME,
        UPDATER_ID, UPDATER_NAME, COMPANY_ID, COMPANY_NAME
    </sql>

    <!-- 根据流程ID查询节点列表 -->
    <select id="selectByFlowId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_BUDGET_APPROVAL_NODE
        WHERE FLOW_ID = #{flowId}
        ORDER BY ORDER_NUM ASC
    </select>

    <!-- 根据节点编码查询 -->
    <select id="selectByNodeCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_BUDGET_APPROVAL_NODE
        WHERE NODE_CODE = #{nodeCode}
    </select>

    <!-- 根据状态查询 -->
    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_BUDGET_APPROVAL_NODE
        WHERE STATUS = #{status}
        ORDER BY CREATE_TIME DESC
    </select>

    <!-- 查询下一个节点 -->
    <select id="selectNextNode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_BUDGET_APPROVAL_NODE
        WHERE FLOW_ID = #{flowId}
        AND ORDER_NUM > #{currentOrderNum}
        ORDER BY ORDER_NUM ASC
        FETCH FIRST 1 ROWS ONLY
    </select>

    <!-- 批量插入节点 -->
    <insert id="batchInsert">
        INSERT INTO TBL_BUDGET_APPROVAL_NODE (
            NODE_ID, FLOW_ID, NODE_NAME, NODE_CODE, NODE_TYPE, APPROVERS,
            APPROVAL_MODE, STATUS, ORDER_NUM, NODE_CONFIG, TIMEOUT_HOURS,
            DESCRIPTION, CREATE_TIME, UPDATE_TIME, CREATOR_ID, CREATOR_NAME,
            COMPANY_ID, COMPANY_NAME
        ) VALUES
        <foreach collection="nodes" item="node" separator=",">
            (
                #{node.nodeId}, #{node.flowId}, #{node.nodeName}, #{node.nodeCode},
                #{node.nodeType}, #{node.approvers}, #{node.approvalMode}, #{node.status},
                #{node.orderNum}, #{node.nodeConfig}, #{node.timeoutHours}, #{node.description},
                #{node.createTime}, #{node.updateTime}, #{node.creatorId}, #{node.creatorName},
                #{node.companyId}, #{node.companyName}
            )
        </foreach>
    </insert>

    <!-- 批量更新状态 -->
    <update id="batchUpdateStatus">
        UPDATE TBL_BUDGET_APPROVAL_NODE
        SET STATUS = #{status},
            UPDATE_TIME = CURRENT_TIMESTAMP
        WHERE NODE_ID IN
        <foreach collection="nodeIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 删除流程的所有节点 -->
    <delete id="deleteByFlowId">
        DELETE FROM TBL_BUDGET_APPROVAL_NODE
        WHERE FLOW_ID = #{flowId}
    </delete>

</mapper>

