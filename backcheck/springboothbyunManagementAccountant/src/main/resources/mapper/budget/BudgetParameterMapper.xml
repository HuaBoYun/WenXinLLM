<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.management.accountant.oracle.mapper.budget.BudgetParameterMapper">

    <select id="selectByParameterCode" resultType="com.management.accountant.oracle.entity.budget.BudgetParameter">
        SELECT * FROM TBL_BUDGET_PARAMETER WHERE PARAMETER_CODE = #{parameterCode} AND DEL_FLAG = 0
    </select>

    <select id="selectByParameterType" resultType="com.management.accountant.oracle.entity.budget.BudgetParameter">
        SELECT * FROM TBL_BUDGET_PARAMETER WHERE PARAMETER_TYPE = #{parameterType} AND DEL_FLAG = 0 ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByParameterCategory" resultType="com.management.accountant.oracle.entity.budget.BudgetParameter">
        SELECT * FROM TBL_BUDGET_PARAMETER WHERE PARAMETER_CATEGORY = #{parameterCategory} AND DEL_FLAG = 0 ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectEnabledParameters" resultType="com.management.accountant.oracle.entity.budget.BudgetParameter">
        SELECT * FROM TBL_BUDGET_PARAMETER WHERE IS_ENABLED = 1 AND DEL_FLAG = 0 ORDER BY CREATE_TIME DESC
    </select>

    <update id="batchDeleteParameters">
        UPDATE TBL_BUDGET_PARAMETER SET DEL_FLAG = 1 WHERE PARAMETER_ID IN
        <foreach collection="parameterIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>

