<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.management.accountant.oracle.mapper.budget.BudgetAllocationMapper">

    <select id="selectByAllocationCode" resultType="com.management.accountant.oracle.entity.budget.BudgetAllocation">
        SELECT * FROM TBL_BUDGET_ALLOCATION WHERE ALLOCATION_CODE = #{allocationCode} AND DEL_FLAG = 0
    </select>

    <select id="selectBySourceBudgetId" resultType="com.management.accountant.oracle.entity.budget.BudgetAllocation">
        SELECT * FROM TBL_BUDGET_ALLOCATION WHERE SOURCE_BUDGET_ID = #{sourceBudgetId} AND DEL_FLAG = 0 ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByTargetBudgetId" resultType="com.management.accountant.oracle.entity.budget.BudgetAllocation">
        SELECT * FROM TBL_BUDGET_ALLOCATION WHERE TARGET_BUDGET_ID = #{targetBudgetId} AND DEL_FLAG = 0 ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByAllocationStatus" resultType="com.management.accountant.oracle.entity.budget.BudgetAllocation">
        SELECT * FROM TBL_BUDGET_ALLOCATION WHERE ALLOCATION_STATUS = #{allocationStatus} AND DEL_FLAG = 0 ORDER BY CREATE_TIME DESC
    </select>

    <update id="batchDeleteAllocations">
        UPDATE TBL_BUDGET_ALLOCATION SET DEL_FLAG = 1 WHERE ALLOCATION_ID IN
        <foreach collection="allocationIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>

