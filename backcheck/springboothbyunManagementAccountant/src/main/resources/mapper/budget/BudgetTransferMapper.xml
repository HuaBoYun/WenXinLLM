<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.management.accountant.oracle.mapper.budget.BudgetTransferMapper">
     <select id="selectByTransferCode" resultType="com.management.accountant.oracle.entity.budget.BudgetTransfer">
        SELECT * FROM TBL_BUDGET_TRANSFER WHERE TRANSFER_CODE = #{transferCode} AND DEL_FLAG = 0
    </select>
     <select id="selectBySourceBudgetId" resultType="com.management.accountant.oracle.entity.budget.BudgetTransfer">
         SELECT * FROM TBL_BUDGET_TRANSFER WHERE SOURCE_BUDGET_ID = #{sourceBudgetId} AND DEL_FLAG = 0 ORDER BY TRANSFER_DATE DESC
    </select>
     <select id="selectByTransferStatus" resultType="com.management.accountant.oracle.entity.budget.BudgetTransfer">
        SELECT * FROM TBL_BUDGET_TRANSFER WHERE TRANSFER_STATUS = #{transferStatus} AND DEL_FLAG = 0 ORDER BY TRANSFER_DATE DESC
    </select>
    <select id="selectPendingTransfers" resultType="com.management.accountant.oracle.entity.budget.BudgetTransfer">
        SELECT * FROM TBL_BUDGET_TRANSFER WHERE TRANSFER_STATUS = 'PENDING' AND DEL_FLAG = 0 ORDER BY CREATE_TIME DESC
    </select>
    <update id="batchDeleteTransfers">
        UPDATE TBL_BUDGET_TRANSFER SET DEL_FLAG = 1, UPDATE_TIME = SYSDATE
        WHERE TRANSFER_ID IN <foreach collection="transferIds" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>
</mapper>

