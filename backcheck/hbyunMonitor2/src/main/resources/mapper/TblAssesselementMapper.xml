<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.huabo.monitor.mapper.TblAssesselementMapper">

	<select id="selectPageInfo"
		resultType="com.huabo.monitor.entity.TblAssesselement">
		select * from TBL_ASSESSELEMENT where 1=1
		<if test="queryParam.tblcomany!=null and queryParam.tblcomany!=''">
			and tblcomany=#{queryParam.tblcomany}
		</if>

		<if
			test="queryParam.elementname!=null and queryParam.elementname!=''">
			and elementname LIKE concat(concat('%',#{queryParam.elementname}),'%')
		</if>
		<if
			test="queryParam.businessattribute!=null and queryParam.businessattribute!=''">
			and businessattribute LIKE
			concat(concat('%',#{queryParam.businessattribute}),'%')
		</if>

		<if
			test="queryParam.businesstype!=null and queryParam.businesstype!=''">
			and businesstype LIKE concat(concat('%',#{queryParam.businesstype}),'%')
		</if>
		<if
			test="queryParam.elementNumber!=null and queryParam.elementNumber!=''">
			and elementNumber LIKE concat('%',#{queryParam.elementnumber},'%')
		</if>
		<if
			test="queryParam.auditpoint!=null and queryParam.auditpoint!=''">
			and auditpoint LIKE concat(concat('%',#{queryParam.auditpoint}),'%')
		</if>

		<if test="sql!=null and sql!=''">
			${sql}
		</if>
		order by asseleid desc
	</select>

</mapper>
