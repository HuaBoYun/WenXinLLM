<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.monitor.mapper.TblStaffMapper">


	<resultMap type="com.huabo.monitor.entity.TblStaff"
		id="staffResult" autoMapping="true">
		<result column="realname" property="realname" />
		<result column="fixedphone" property="fixedphone" />
		<result column="address" property="address" />
		<result column="email" property="email" />
		<result column="miblephone" property="miblephone" />
		<result column="memo" property="memo" />
		<result column="orgid" property="orgid" />
		<result column="username" property="username" />
		<result column="password" property="password" />
		<result column="status" property="status" />
		<result column="roleid" property="roleid" />

	</resultMap>

	<select id="findStaffByOrgid"
		resultType="com.huabo.monitor.entity.TblStaff">
		select * from tbl_staff where 1=1
		<if test="queryParam.orgid!=null">
			and orgid = #{queryParam.orgid}
		</if>
		AND ( status is NULL or status != 0)
	</select>

	<select id="getOrgNameByIds"
		resultType="com.huabo.monitor.entity.TblStaff">
		select realname from tbl_staff where 1=1
		<if test="ids!=null and ids.length>0">
			and staffid IN
			<foreach collection="ids" open="(" close=")" item="item"
				separator=",">
				#{item}
			</foreach>
		</if>
	</select>

</mapper>
