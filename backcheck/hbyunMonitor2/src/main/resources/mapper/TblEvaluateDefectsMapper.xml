<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.huabo.monitor.mapper.TblEvaluateDefectsMapper">

	<select id="getHomepage_List"
		resultType="com.huabo.monitor.entity.TblEvaluateDefects">
		select * from TBL_EVALUATEDEFECTS where 1=1
		<if test="param.defectsname!=null and param.defectsname!=''">
			and defectsname LIKE concat(concat('%',#{param.defectsname}),'%')
		</if>
		<if test="param.defectscode!=null and param.defectscode!=''">
			and defectscode LIKE concat(concat('%',#{param.defectscode}),'%')
		</if>
		<if test="authorityType==0">
			and createstaffid=#{param.createstaffid}
		</if>
		<if test="param.unit!=null and param.unit!=''">
			and unit=#{param.unit}
		</if>
		order by id desc
	</select>


</mapper>
