<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.audit.oracle.mapper.TblNbsjBorrowRecordMapper">



  <select id="selectListByPageInfoXml" resultType="com.huabo.audit.oracle.entity.TblNbsjBorrowRecordEntity">
      SELECT TNA.BORROWID,TNA.CREATEDATE,TNA.RETURNDATE,TNA.MEMO,staff.REALNAME
      FROM TBL_NBSJ_BORROWRECORD TNA
      LEFT JOIN TBL_STAFF staff ON staff.STAFFID = TNA.STAFFID
      <where>
          TNA.PROJECTID= #{projectid}
      </where>
      ORDER BY TNA.BORROWID DESC
  </select>


</mapper>


