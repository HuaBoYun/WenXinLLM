<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.audit.oracle.mapper.TblNbsjInnerruleMapper">

  <select id="selectInnerCommonListByLinkXml" resultType="com.huabo.audit.oracle.entity.TblNbsjInnerrule">
      SELECT TNA.innrulid,TNA.rulecode,TNA.rulename,TNA.publishorg,TNA.publishdate,TNA.rulenumber,ORG.ORGNAME
      FROM TBL_NBSJ_INNERRULE TNA
          LEFT JOIN TBL_ORGANIZATION ORG ON ORG.ORGID = TNA.publishorg
          LEFT JOIN TBL_NBSJ_BUG_INNERRULE TBI on TNA.INNRULID=TBI.INNRULID
          LEFT JOIN TBL_NBSJ_BUG TBUG on TBUG.BUGID=TBI.BUGID
      WHERE TBUG.BUGID = #{bugid} ORDER BY TNA.INNRULID DESC
  </select>

  <select id="selectInnerCommonListByPageInfoXml" resultType="com.huabo.audit.oracle.entity.TblNbsjInnerrule">
        SELECT TNA.innrulid,TNA.rulecode,TNA.rulename,TNA.publishorg,TNA.publishdate,TNA.rulenumber,ORG.ORGNAME
        FROM TBL_NBSJ_INNERRULE TNA
                 LEFT JOIN TBL_ORGANIZATION ORG ON ORG.ORGID = TNA.publishorg
        WHERE INNRULID not in
              (SELECT DISTINCT INNRULID from TBL_NBSJ_BUG_INNERRULE where bugid=#{bugid})
          and PUBLISHORG in
              ( select to_char(ORGID) from TBL_ORGANIZATION where (1 = 1  AND orgtype=0 and (STATUS != 1 or STATUS IS NULL) )
                       or  ORGID =#{orgid} start with  ORGID= #{orgid} connect by prior ORGID= fatherorgid) ORDER BY TNA.INNRULID DESC
  </select>
    

</mapper>
