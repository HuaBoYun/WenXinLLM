<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.central.enterprises.audit.oracle.mapper.TblCeaExamineTopicMapper">
    <select id="getList" resultType="com.huabo.central.enterprises.audit.oracle.entity.TblCeaExamineTopic">
        select distinct a.SCORETRANSACTION,a.EXAMINETYPE,a.TOPICNAME,max(a.id) as max
        from TBL_CEA_EXAMINE_TOPIC a
        where 1=1
        <if test="param.examineType != null">
            and a.examineType = #{param.examineType}
        </if>
        group by a.SCORETRANSACTION,a.EXAMINETYPE,a.TOPICNAME
        order by max desc
    </select>
</mapper>