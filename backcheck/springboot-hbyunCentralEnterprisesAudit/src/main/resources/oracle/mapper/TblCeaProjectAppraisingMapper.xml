<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.central.enterprises.audit.oracle.mapper.TblCeaProjectAppraisingMapper">

    <select id="findImplementationPlanList" resultType="com.huabo.central.enterprises.audit.vo.result.ImplementationPlanResult">
        select t1.ID as implementationPlanId,t1.PLAN_NAME as implementationProjectName,t1.PROJECT_ORDER_NAME as implementationPlanMainReviewer,
        t1.PROJECT_TYPE as implementationProjectType,t1.PROJECT_ORDER_ID as implementationProjectOrderId
        from TBL_YQNS_IMPLEMENT_PLAN t1
        where t1.id in
        <foreach collection="implementationPlanId" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
        order by t1.id desc
    </select>

</mapper>