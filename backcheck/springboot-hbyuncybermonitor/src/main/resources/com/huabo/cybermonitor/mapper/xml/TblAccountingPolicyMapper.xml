<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.cybermonitor.mapper.TblAccountingPolicyMapper">

    <select id="selectPageList" resultType="com.huabo.cybermonitor.entity.TblAccountingPolicy">
        SELECT * FROM TBL_ACCOUNTING_POLICY
        <where>
            <if test="query.companyId != null and query.companyId != ''">AND COMPANY_ID = #{query.companyId}</if>
            <if test="query.policyType != null and query.policyType != ''">AND POLICY_TYPE = #{query.policyType}</if>
            <if test="query.policyName != null and query.policyName != ''">AND POLICY_NAME LIKE CONCAT('%', #{query.policyName}, '%')</if>
        </where>
        ORDER BY CREATE_TIME DESC
    </select>

    <!-- 政策一致性对照查询 -->
    <select id="selectPolicyComparison" resultType="java.util.Map">
        SELECT POLICY_TYPE, COUNT(DISTINCT POLICY_CONTENT) AS variantCount, COUNT(*) AS companyCount
        FROM TBL_ACCOUNTING_POLICY WHERE STATUS = 'ACTIVE'
        GROUP BY POLICY_TYPE HAVING COUNT(DISTINCT POLICY_CONTENT) > 1
    </select>

</mapper>

