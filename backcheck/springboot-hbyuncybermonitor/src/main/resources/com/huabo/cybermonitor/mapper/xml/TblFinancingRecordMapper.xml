<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.cybermonitor.mapper.TblFinancingRecordMapper">

    <select id="selectPageList" resultType="com.huabo.cybermonitor.entity.TblFinancingRecord">
        SELECT * FROM TBL_FINANCING_RECORD
        <where>
            <if test="query.companyId != null and query.companyId != ''">AND COMPANY_ID = #{query.companyId}</if>
            <if test="query.financingType != null and query.financingType != ''">AND FINANCING_TYPE = #{query.financingType}</if>
            <if test="query.financingName != null and query.financingName != ''">AND FINANCING_NAME LIKE CONCAT('%', #{query.financingName}, '%')</if>
        </where>
        ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectFinancingStats" resultType="java.util.Map">
        SELECT COMPANY_ID, COUNT(*) AS cnt, SUM(FINANCING_AMOUNT) AS totalAmount,
               SUM(CASE WHEN STATUS = 'ACTIVE' THEN FINANCING_AMOUNT ELSE 0 END) AS activeAmount
        FROM TBL_FINANCING_RECORD
        <where><if test="companyId != null and companyId != ''">AND COMPANY_ID = #{companyId}</if></where>
        GROUP BY COMPANY_ID
    </select>

</mapper>

