<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.cybermonitor.mapper.TblSalaryTotalMapper">

    <select id="selectPageList" resultType="com.huabo.cybermonitor.entity.TblSalaryTotal">
        SELECT * FROM TBL_SALARY_TOTAL
        <where>
            <if test="query.companyId != null and query.companyId != ''">AND COMPANY_ID = #{query.companyId}</if>
            <if test="query.year != null and query.year != ''">AND YEAR = #{query.year}</if>
            <if test="query.companyName != null and query.companyName != ''">AND COMPANY_NAME LIKE CONCAT('%', #{query.companyName}, '%')</if>
        </where>
        ORDER BY YEAR DESC, CREATE_TIME DESC
    </select>

    <!-- 效益联动分析 -->
    <select id="selectPerformanceLinkage" resultType="java.util.Map">
        SELECT COMPANY_ID, COMPANY_NAME, YEAR, TOTAL_SALARY, AVERAGE_SALARY, EMPLOYEE_COUNT,
               LABOR_PRODUCTIVITY, PROFIT_PER_CAPITA
        FROM TBL_SALARY_TOTAL
        <where><if test="companyId != null and companyId != ''">AND COMPANY_ID = #{companyId}</if></where>
        ORDER BY YEAR DESC
    </select>

</mapper>

