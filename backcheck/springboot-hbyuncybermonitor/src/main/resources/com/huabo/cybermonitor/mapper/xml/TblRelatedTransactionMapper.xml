<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.cybermonitor.mapper.TblRelatedTransactionMapper">

    <select id="selectPageList" resultType="com.huabo.cybermonitor.entity.TblRelatedTransaction">
        SELECT * FROM TBL_RELATED_TRANSACTION
        <where>
            <if test="query.companyId != null and query.companyId != ''">AND COMPANY_ID = #{query.companyId}</if>
            <if test="query.transactionType != null and query.transactionType != ''">AND TRANSACTION_TYPE = #{query.transactionType}</if>
            <if test="query.relatedPartyName != null and query.relatedPartyName != ''">AND RELATED_PARTY_NAME LIKE CONCAT('%', #{query.relatedPartyName}, '%')</if>
        </where>
        ORDER BY TRANSACTION_DATE DESC
    </select>

</mapper>

