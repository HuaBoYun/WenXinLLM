# 华博云数据修复执行指南

## 执行前准备

### 1. 环境检查
```sql
-- 检查数据库连接
SELECT USER, SYSDATE FROM DUAL;

-- 检查表空间状态
SELECT TABLESPACE_NAME, STATUS FROM USER_TABLESPACES;

-- 检查当前会话
SELECT SID, SERIAL#, STATUS FROM V$SESSION WHERE AUDSID = USERENV('SESSIONID');
```

### 2. 权限验证
确保执行用户具有以下权限：
- 对 `T_INTERNAL_SETTLEMENT` 表的 SELECT, INSERT, UPDATE, DELETE 权限
- 对 `REDACTED.T_RESPONSIBILITY_CENTER` 表的 SELECT, INSERT, UPDATE, DELETE 权限
- 对 `T_COST_CENTER` 表的 SELECT 权限
- CREATE TABLE 权限（用于备份）

### 3. 执行时间窗口
建议在业务低峰期执行，通常：
- 工作日：21:00 - 23:00
- 周末：任意时间

## 执行步骤

### 步骤1：数据备份（必须执行）

```sql
-- 步骤1.1：备份结算表
CREATE TABLE T_INTERNAL_SETTLEMENT_BACKUP_20251203 AS
SELECT * FROM T_INTERNAL_SETTLEMENT;

-- 步骤1.2：备份责任中心表
CREATE TABLE T_RESPONSIBILITY_CENTER_BACKUP_20251203 AS
SELECT * FROM REDACTED.T_RESPONSIBILITY_CENTER;

-- 步骤1.3：验证备份成功
SELECT 'T_INTERNAL_SETTLEMENT' as table_name, COUNT(*) as record_count FROM T_INTERNAL_SETTLEMENT
UNION ALL
SELECT 'T_INTERNAL_SETTLEMENT_BACKUP_20251203', COUNT(*) FROM T_INTERNAL_SETTLEMENT_BACKUP_20251203
UNION ALL
SELECT 'T_RESPONSIBILITY_CENTER', COUNT(*) FROM REDACTED.T_RESPONSIBILITY_CENTER
UNION ALL
SELECT 'T_RESPONSIBILITY_CENTER_BACKUP_20251203', COUNT(*) FROM T_RESPONSIBILITY_CENTER_BACKUP_20251203;

COMMIT;
```

### 步骤2：问题数据分析

```sql
-- 步骤2.1：统计问题数据
SELECT
    '问题数据统计' as 统计类型,
    COUNT(*) as total_settlements,
    SUM(CASE WHEN r_from.CENTER_ID IS NULL THEN 1 ELSE 0 END) as invalid_from_centers,
    SUM(CASE WHEN r_to.CENTER_ID IS NULL THEN 1 ELSE 0 END) as invalid_to_centers,
    SUM(CASE WHEN r_from.CENTER_ID IS NULL AND r_to.CENTER_ID IS NULL THEN 1 ELSE 0 END) as both_invalid
FROM T_INTERNAL_SETTLEMENT s
LEFT JOIN REDACTED.T_RESPONSIBILITY_CENTER r_from ON s.FROM_CENTER_ID = r_from.CENTER_ID
LEFT JOIN REDACTED.T_RESPONSIBILITY_CENTER r_to ON s.TO_CENTER_ID = r_to.CENTER_ID
WHERE r_from.CENTER_ID IS NULL OR r_to.CENTER_ID IS NULL;

-- 步骤2.2：查看具体问题记录（前10条）
SELECT
    s.SETTLEMENT_ID,
    s.SETTLEMENT_NO,
    s.FROM_CENTER_ID,
    s.TO_CENTER_ID,
    s.CREATE_TIME,
    CASE
        WHEN r_from.CENTER_ID IS NULL THEN 'FROM_CENTER_ID 不存在'
        WHEN r_to.CENTER_ID IS NULL THEN 'TO_CENTER_ID 不存在'
        ELSE '其他问题'
    END as ISSUE_TYPE
FROM T_INTERNAL_SETTLEMENT s
LEFT JOIN REDACTED.T_RESPONSIBILITY_CENTER r_from ON s.FROM_CENTER_ID = r_from.CENTER_ID
LEFT JOIN REDACTED.T_RESPONSIBILITY_CENTER r_to ON s.TO_CENTER_ID = r_to.CENTER_ID
WHERE r_from.CENTER_ID IS NULL OR r_to.CENTER_ID IS NULL
ORDER BY s.CREATE_TIME DESC
FETCH FIRST 10 ROWS ONLY;
```

### 步骤3：执行数据修复

```sql
-- 步骤3.1：首先查看需要创建的责任中心记录
SELECT COUNT(*) as centers_to_create
FROM T_COST_CENTER c
WHERE c.CENTER_ID IN (
    SELECT DISTINCT FROM_CENTER_ID
    FROM T_INTERNAL_SETTLEMENT
    WHERE FROM_CENTER_ID NOT IN (
        SELECT CENTER_ID FROM REDACTED.T_RESPONSIBILITY_CENTER
    )
    UNION
    SELECT DISTINCT TO_CENTER_ID
    FROM T_INTERNAL_SETTLEMENT
    WHERE TO_CENTER_ID NOT IN (
        SELECT CENTER_ID FROM REDACTED.T_RESPONSIBILITY_CENTER
    )
)
AND c.IS_DELETED = 0 AND c.IS_ENABLED = 1;

-- 步骤3.2：执行插入操作
INSERT INTO REDACTED.T_RESPONSIBILITY_CENTER (
    CENTER_ID,
    CENTER_CODE,
    CENTER_NAME,
    CENTER_TYPE,
    PARENT_CENTER_ID,
    CENTER_LEVEL,
    IS_LEAF,
    MANAGER_ID,
    IS_ENABLED,
    BOOK_ID,
    TENANT_ID,
    VERSION,
    IS_DELETED,
    CREATE_TIME,
    UPDATE_TIME,
    CREATOR,
    UPDATER
)
SELECT
    c.CENTER_ID,
    c.CENTER_CODE,
    c.CENTER_NAME,
    CASE c.CENTER_TYPE
        WHEN 1 THEN 1  -- 成本中心对应责任中心类型1
        WHEN 2 THEN 2  -- 利润中心对应责任中心类型2
        WHEN 3 THEN 3  -- 投资中心对应责任中心类型3
        ELSE 1         -- 默认为类型1
    END as CENTER_TYPE,
    c.PARENT_CENTER_ID,
    c.CENTER_LEVEL,
    c.IS_LEAF,
    c.MANAGER_ID,
    c.IS_ENABLED,
    c.BOOK_ID,
    c.TENANT_ID,
    1 as VERSION,
    0 as IS_DELETED,
    SYSDATE as CREATE_TIME,
    SYSDATE as UPDATE_TIME,
    1 as CREATOR,
    1 as UPDATER
FROM T_COST_CENTER c
WHERE c.CENTER_ID IN (
    SELECT DISTINCT FROM_CENTER_ID
    FROM T_INTERNAL_SETTLEMENT
    WHERE FROM_CENTER_ID NOT IN (
        SELECT CENTER_ID FROM REDACTED.T_RESPONSIBILITY_CENTER
    )
    UNION
    SELECT DISTINCT TO_CENTER_ID
    FROM T_INTERNAL_SETTLEMENT
    WHERE TO_CENTER_ID NOT IN (
        SELECT CENTER_ID FROM REDACTED.T_RESPONSIBILITY_CENTER
    )
)
AND c.IS_DELETED = 0 AND c.IS_ENABLED = 1;

-- 步骤3.3：检查插入结果
SELECT SQL%ROWCOUNT as inserted_rows FROM DUAL;

COMMIT;
```

### 步骤4：修复结果验证

```sql
-- 步骤4.1：验证问题是否解决
SELECT
    '修复后验证' as 验证类型,
    COUNT(*) as total_settlements,
    SUM(CASE WHEN r_from.CENTER_ID IS NULL THEN 1 ELSE 0 END) as invalid_from_centers,
    SUM(CASE WHEN r_to.CENTER_ID IS NULL THEN 1 ELSE 0 END) as invalid_to_centers
FROM T_INTERNAL_SETTLEMENT s
LEFT JOIN REDACTED.T_RESPONSIBILITY_CENTER r_from ON s.FROM_CENTER_ID = r_from.CENTER_ID
LEFT JOIN REDACTED.T_RESPONSIBILITY_CENTER r_to ON s.TO_CENTER_ID = r_to.CENTER_ID;

-- 步骤4.2：查看新创建的责任中心
SELECT COUNT(*) as new_responsibility_centers
FROM REDACTED.T_RESPONSIBILITY_CENTER r
WHERE r.CENTER_ID IN (
    SELECT FROM_CENTER_ID FROM T_INTERNAL_SETTLEMENT
    UNION
    SELECT TO_CENTER_ID FROM T_INTERNAL_SETTLEMENT
)
AND r.CREATE_TIME >= SYSDATE - 1/24;

-- 步骤4.3：最终检查（应该返回0行）
SELECT COUNT(*) as remaining_issues
FROM T_INTERNAL_SETTLEMENT s
LEFT JOIN REDACTED.T_RESPONSIBILITY_CENTER r_from ON s.FROM_CENTER_ID = r_from.CENTER_ID
LEFT JOIN REDACTED.T_RESPONSIBILITY_CENTER r_to ON s.TO_CENTER_ID = r_to.CENTER_ID
WHERE r_from.CENTER_ID IS NULL OR r_to.CENTER_ID IS NULL;
```

## 异常处理

### 如果插入失败

```sql
-- 检查是否有主键冲突
SELECT CENTER_ID, COUNT(*) as duplicate_count
FROM REDACTED.T_RESPONSIBILITY_CENTER
WHERE CENTER_ID IN (
    SELECT DISTINCT FROM_CENTER_ID FROM T_INTERNAL_SETTLEMENT
    UNION
    SELECT DISTINCT TO_CENTER_ID FROM T_INTERNAL_SETTLEMENT
)
GROUP BY CENTER_ID
HAVING COUNT(*) > 1;

-- 如果有冲突，更新现有记录而不是插入
UPDATE REDACTED.T_RESPONSIBILITY_CENTER r
SET (CENTER_CODE, CENTER_NAME, CENTER_TYPE, IS_ENABLED) =
    (SELECT c.CENTER_CODE, c.CENTER_NAME,
            CASE c.CENTER_TYPE WHEN 1 THEN 1 WHEN 2 THEN 2 WHEN 3 THEN 3 ELSE 1 END,
            c.IS_ENABLED
     FROM T_COST_CENTER c
     WHERE c.CENTER_ID = r.CENTER_ID
     AND c.IS_DELETED = 0 AND c.IS_ENABLED = 1)
WHERE r.CENTER_ID IN (
    SELECT DISTINCT FROM_CENTER_ID FROM T_INTERNAL_SETTLEMENT
    UNION
    SELECT DISTINCT TO_CENTER_ID FROM T_INTERNAL_SETTLEMENT
);
```

### 如果需要回滚

```sql
-- 紧急回滚脚本
-- 1. 删除新插入的责任中心记录
DELETE FROM REDACTED.T_RESPONSIBILITY_CENTER
WHERE CENTER_ID IN (
    SELECT CENTER_ID FROM T_COST_CENTER c
    WHERE c.CENTER_ID IN (
        SELECT FROM_CENTER_ID FROM T_INTERNAL_SETTLEMENT_BACKUP_20251203
        UNION
        SELECT TO_CENTER_ID FROM T_INTERNAL_SETTLEMENT_BACKUP_20251203
    )
    AND c.CENTER_ID NOT IN (
        SELECT CENTER_ID FROM T_RESPONSIBILITY_CENTER_BACKUP_20251203
    )
);

-- 2. 恢复原始结算数据
DELETE FROM T_INTERNAL_SETTLEMENT;
INSERT INTO T_INTERNAL_SETTLEMENT SELECT * FROM T_INTERNAL_SETTLEMENT_BACKUP_20251203;

COMMIT;

-- 3. 验证回滚成功
SELECT '回滚后检查' as status, COUNT(*) as record_count FROM T_INTERNAL_SETTLEMENT;
```

## 执行后操作

### 1. 重启应用服务
```bash
# 重启财务共享服务
cd /Users/Zhuanz/devs/ai-code/hbyun-cloud/springboothbyunFinancialSharing
./restart.sh

# 或者使用Maven重启
mvn spring-boot:stop
mvn spring-boot:run -Dspring.profiles.active=local
```

### 2. 清理备份表（确认修复成功后执行）
```sql
-- 可以在7天后删除备份表
-- DROP TABLE T_INTERNAL_SETTLEMENT_BACKUP_20251203;
-- DROP TABLE T_RESPONSIBILITY_CENTER_BACKUP_20251203;
```

### 3. 记录修复结果
```sql
INSERT INTO OPERATION_LOG (LOG_ID, OPERATION_TYPE, OPERATION_TIME, OPERATOR, DESCRIPTION)
VALUES (
    SYS_GUID(),
    'DATA_FIX_COMPLETED',
    SYSDATE,
    'DBA',
    'T_INTERNAL_SETTLEMENT 外键约束问题修复完成'
);

COMMIT;
```

## 验证清单

- [ ] 数据备份完成
- [ ] 问题数据分析完成
- [ ] 修复脚本执行成功
- [ ] 外键约束问题解决
- [ ] 新创建的责任中心记录验证
- [ ] 应用服务重启成功
- [ ] 业务功能测试通过
- [ ] 修复操作记录完整

## 联系信息

如果在执行过程中遇到问题，请联系：
- 数据库管理员：[DBA联系方式]
- 应用运维：[运维联系方式]
- 开发团队：[开发联系方式]