<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.business.mapper.PrepaymentRefundMapper">

    <!-- 根据预付款单ID查询退款记录 -->
    <select id="selectByPrepaymentId" resultType="com.financial.sharing.business.entity.TblPrepaymentRefund">
        SELECT *
        FROM TBL_PREPAYMENT_REFUND
        WHERE PREPAYMENT_ID = #{prepaymentId}
        ORDER BY REFUND_DATE DESC
    </select>

    <!-- 统计退款金额 -->
    <select id="sumRefundAmount" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(REFUND_AMOUNT), 0)
        FROM TBL_PREPAYMENT_REFUND
        WHERE PREPAYMENT_ID = #{prepaymentId}
    </select>

</mapper>
