<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.business.mapper.ExpenseProvisionReverseMapper">

    <!-- 根据预提单ID查询冲销记录 -->
    <select id="selectByProvisionId" resultType="com.financial.sharing.business.entity.TblExpenseProvisionReverse">
        SELECT *
        FROM TBL_EXPENSE_PROVISION_REVERSE
        WHERE PROVISION_ID = #{provisionId}
        ORDER BY REVERSE_DATE DESC
    </select>

    <!-- 统计冲销金额 -->
    <select id="sumReverseAmount" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(REVERSE_AMOUNT), 0)
        FROM TBL_EXPENSE_PROVISION_REVERSE
        WHERE PROVISION_ID = #{provisionId}
    </select>

</mapper>
