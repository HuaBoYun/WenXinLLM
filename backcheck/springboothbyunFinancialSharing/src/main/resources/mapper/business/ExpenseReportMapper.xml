<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.business.mapper.ExpenseReportMapper">

    <select id="selectByReportCode" resultType="com.financial.sharing.business.entity.TblExpenseReport">
        SELECT * FROM TBL_EXPENSE_REPORT WHERE REPORT_CODE = #{reportCode}
    </select>

    <select id="selectByApplicantId" resultType="com.financial.sharing.business.entity.TblExpenseReport">
        SELECT * FROM TBL_EXPENSE_REPORT WHERE APPLICANT_ID = #{applicantId} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByReportStatus" resultType="com.financial.sharing.business.entity.TblExpenseReport">
        SELECT * FROM TBL_EXPENSE_REPORT WHERE REPORT_STATUS = #{reportStatus} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByDeptId" resultType="com.financial.sharing.business.entity.TblExpenseReport">
        SELECT * FROM TBL_EXPENSE_REPORT WHERE APPLICANT_DEPT_ID = #{deptId} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByReportType" resultType="com.financial.sharing.business.entity.TblExpenseReport">
        SELECT * FROM TBL_EXPENSE_REPORT WHERE REPORT_TYPE = #{reportType} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectPendingReports" resultType="com.financial.sharing.business.entity.TblExpenseReport">
        SELECT * FROM TBL_EXPENSE_REPORT WHERE REPORT_STATUS = 'PENDING' ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByDateRange" resultType="com.financial.sharing.business.entity.TblExpenseReport">
        SELECT * FROM TBL_EXPENSE_REPORT 
        WHERE CREATE_TIME &gt;= #{startDate} AND CREATE_TIME &lt;= #{endDate}
        ORDER BY CREATE_TIME DESC
    </select>

    <select id="sumTotalAmount" resultType="java.math.BigDecimal">
        SELECT NVL(SUM(TOTAL_AMOUNT), 0) FROM TBL_EXPENSE_REPORT 
        WHERE APPLICANT_ID = #{applicantId}
        AND CREATE_TIME &gt;= #{startDate} AND CREATE_TIME &lt;= #{endDate}
    </select>

    <select id="selectUnpaidReports" resultType="com.financial.sharing.business.entity.TblExpenseReport">
        SELECT * FROM TBL_EXPENSE_REPORT 
        WHERE REPORT_STATUS = 'APPROVED' AND PAYMENT_STATUS = 'UNPAID'
        ORDER BY CREATE_TIME DESC
    </select>

</mapper>
