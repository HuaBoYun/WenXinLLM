<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.business.mapper.PrepaymentWriteoffMapper">

    <!-- 根据预付款单ID查询核销记录 -->
    <select id="selectByPrepaymentId" resultType="com.financial.sharing.business.entity.TblPrepaymentWriteoff">
        SELECT *
        FROM TBL_PREPAYMENT_WRITEOFF
        WHERE PREPAYMENT_ID = #{prepaymentId}
        ORDER BY WRITEOFF_DATE DESC
    </select>

    <!-- 统计已核销金额 -->
    <select id="sumWriteoffAmount" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(WRITEOFF_AMOUNT), 0)
        FROM TBL_PREPAYMENT_WRITEOFF
        WHERE PREPAYMENT_ID = #{prepaymentId}
    </select>

</mapper>
