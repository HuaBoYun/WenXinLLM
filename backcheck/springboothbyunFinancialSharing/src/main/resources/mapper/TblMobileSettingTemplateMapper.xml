<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblMobileSettingTemplateMapper">

    <select id="selectByTemplateName" resultType="com.financial.sharing.entity.TblMobileSettingTemplate">
        SELECT * FROM TBL_MOBILE_SETTING_TEMPLATE WHERE TEMPLATE_NAME LIKE CONCAT('%', #{templateName}, '%') ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByPlatform" resultType="com.financial.sharing.entity.TblMobileSettingTemplate">
        SELECT * FROM TBL_MOBILE_SETTING_TEMPLATE WHERE PLATFORM = #{platform} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByIsEnabled" resultType="com.financial.sharing.entity.TblMobileSettingTemplate">
        SELECT * FROM TBL_MOBILE_SETTING_TEMPLATE WHERE IS_ENABLED = #{isEnabled} ORDER BY CREATE_TIME DESC
    </select>

</mapper>
