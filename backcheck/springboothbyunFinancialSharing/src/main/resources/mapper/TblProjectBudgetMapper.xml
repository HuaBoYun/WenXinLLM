<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblProjectBudgetMapper">

    <select id="selectByProjectId" resultType="com.financial.sharing.entity.TblProjectBudget">
        SELECT * FROM TBL_PROJECT_BUDGET WHERE PROJECT_ID = #{projectId} AND IS_ENABLED = 1 ORDER BY YEAR DESC, MONTH DESC
    </select>

    <select id="selectByProjectAndYearMonth" resultType="com.financial.sharing.entity.TblProjectBudget">
        SELECT * FROM TBL_PROJECT_BUDGET WHERE PROJECT_ID = #{projectId} AND YEAR = #{year} AND MONTH = #{month} LIMIT 1
    </select>

    <select id="selectByBudgetCode" resultType="com.financial.sharing.entity.TblProjectBudget">
        SELECT * FROM TBL_PROJECT_BUDGET WHERE BUDGET_CODE = #{budgetCode} LIMIT 1
    </select>

    <delete id="deleteByProjectId">
        DELETE FROM TBL_PROJECT_BUDGET WHERE PROJECT_ID = #{projectId}
    </delete>

</mapper>

