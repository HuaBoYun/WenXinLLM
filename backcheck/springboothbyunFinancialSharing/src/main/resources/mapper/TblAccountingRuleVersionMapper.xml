<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblAccountingRuleVersionMapper">

    <select id="selectByRuleId" resultType="com.financial.sharing.entity.TblAccountingRuleVersion">
        SELECT * FROM TBL_ACCOUNTING_RULE_VERSION WHERE RULE_ID = #{ruleId} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByVersionNumber" resultType="com.financial.sharing.entity.TblAccountingRuleVersion">
        SELECT * FROM TBL_ACCOUNTING_RULE_VERSION WHERE VERSION_NUMBER = #{versionNumber} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectLatestByRuleId" resultType="com.financial.sharing.entity.TblAccountingRuleVersion">
        SELECT * FROM TBL_ACCOUNTING_RULE_VERSION
        WHERE RULE_ID = #{ruleId}
        ORDER BY CREATE_TIME DESC
        FETCH FIRST 1 ROWS ONLY
    </select>

</mapper>
