<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblProxyDelegationLogMapper">

    <select id="selectByDelegationId" resultType="com.financial.sharing.entity.TblProxyDelegationLog">
        SELECT * FROM TBL_PROXY_DELEGATION_LOG WHERE DELEGATION_ID = #{delegationId} ORDER BY OPERATION_TIME DESC
    </select>

    <select id="selectByOperator" resultType="com.financial.sharing.entity.TblProxyDelegationLog">
        SELECT * FROM TBL_PROXY_DELEGATION_LOG WHERE OPERATOR = #{operator} ORDER BY OPERATION_TIME DESC
    </select>

    <select id="selectByBusinessType" resultType="com.financial.sharing.entity.TblProxyDelegationLog">
        SELECT * FROM TBL_PROXY_DELEGATION_LOG WHERE BUSINESS_TYPE = #{businessType} ORDER BY OPERATION_TIME DESC
    </select>

    <select id="selectByBusinessId" resultType="com.financial.sharing.entity.TblProxyDelegationLog">
        SELECT * FROM TBL_PROXY_DELEGATION_LOG WHERE BUSINESS_ID = #{businessId} ORDER BY OPERATION_TIME DESC
    </select>

    <select id="selectByDelegationIdAndBusinessType" resultType="com.financial.sharing.entity.TblProxyDelegationLog">
        SELECT * FROM TBL_PROXY_DELEGATION_LOG
        WHERE DELEGATION_ID = #{delegationId} AND BUSINESS_TYPE = #{businessType}
        ORDER BY OPERATION_TIME DESC
    </select>

</mapper>
