<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblProjectSettlementMapper">

    <select id="selectByProjectId" resultType="com.financial.sharing.entity.TblProjectSettlement">
        SELECT * FROM TBL_PROJECT_SETTLEMENT WHERE PROJECT_ID = #{projectId} ORDER BY SETTLEMENT_DATE DESC
    </select>

    <select id="selectBySettlementStatus" resultType="com.financial.sharing.entity.TblProjectSettlement">
        SELECT * FROM TBL_PROJECT_SETTLEMENT WHERE SETTLEMENT_STATUS = #{settlementStatus} ORDER BY SETTLEMENT_DATE DESC
    </select>

    <select id="selectLatestSettlement" resultType="com.financial.sharing.entity.TblProjectSettlement">
        SELECT * FROM TBL_PROJECT_SETTLEMENT WHERE PROJECT_ID = #{projectId} ORDER BY SETTLEMENT_DATE DESC LIMIT 1
    </select>

</mapper>

