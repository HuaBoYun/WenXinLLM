<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblExpenseParameterOptionMapper">

    <select id="selectByParameterId" resultType="com.financial.sharing.entity.TblExpenseParameterOption">
        SELECT * FROM TBL_EXPENSE_PARAMETER_OPTION WHERE PARAMETER_ID = #{parameterId} ORDER BY SORT_ORDER ASC
    </select>

    <select id="selectByIsEnabled" resultType="com.financial.sharing.entity.TblExpenseParameterOption">
        SELECT * FROM TBL_EXPENSE_PARAMETER_OPTION WHERE IS_ENABLED = #{isEnabled} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByParameterIdAndEnabled" resultType="com.financial.sharing.entity.TblExpenseParameterOption">
        SELECT * FROM TBL_EXPENSE_PARAMETER_OPTION
        WHERE PARAMETER_ID = #{parameterId} AND IS_ENABLED = #{isEnabled}
        ORDER BY SORT_ORDER ASC
    </select>

</mapper>
