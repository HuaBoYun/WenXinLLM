<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblTravelArchiveAgreementMapper">

    <select id="selectByArchiveId" resultType="com.financial.sharing.entity.TblTravelArchiveAgreement">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_AGREEMENT WHERE ARCHIVE_ID = #{archiveId} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByAgreementNameLike" resultType="com.financial.sharing.entity.TblTravelArchiveAgreement">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_AGREEMENT
        WHERE AGREEMENT_NAME LIKE CONCAT('%', #{agreementName}, '%')
        ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByIsEnabled" resultType="com.financial.sharing.entity.TblTravelArchiveAgreement">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_AGREEMENT WHERE IS_ENABLED = #{isEnabled} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByContentType" resultType="com.financial.sharing.entity.TblTravelArchiveAgreement">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_AGREEMENT WHERE CONTENT_TYPE = #{contentType} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByArchiveIdAndIsEnabled" resultType="com.financial.sharing.entity.TblTravelArchiveAgreement">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_AGREEMENT
        WHERE ARCHIVE_ID = #{archiveId} AND IS_ENABLED = #{isEnabled}
        ORDER BY CREATE_TIME DESC
    </select>

</mapper>
