<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblGeneralStandardConditionMapper">

    <select id="selectByStandardId" resultType="com.financial.sharing.entity.TblGeneralStandardCondition">
        SELECT * FROM TBL_GENERAL_STANDARD_CONDITION WHERE STANDARD_ID = #{standardId} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByStandardIdOrderByPriority" resultType="com.financial.sharing.entity.TblGeneralStandardCondition">
        SELECT * FROM TBL_GENERAL_STANDARD_CONDITION
        WHERE STANDARD_ID = #{standardId}
        ORDER BY PRIORITY ASC
    </select>

    <select id="selectByConditionExpressionLike" resultType="com.financial.sharing.entity.TblGeneralStandardCondition">
        SELECT * FROM TBL_GENERAL_STANDARD_CONDITION
        WHERE CONDITION_EXPRESSION LIKE CONCAT('%', #{conditionExpression}, '%')
        ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByStandardIdAndPriority" resultType="com.financial.sharing.entity.TblGeneralStandardCondition">
        SELECT * FROM TBL_GENERAL_STANDARD_CONDITION
        WHERE STANDARD_ID = #{standardId} AND PRIORITY = #{priority}
        ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectMaxPriorityByStandardId" resultType="java.lang.Integer">
        SELECT MAX(PRIORITY) FROM TBL_GENERAL_STANDARD_CONDITION WHERE STANDARD_ID = #{standardId}
    </select>

</mapper>
