<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.consolidationReport.mapper.ConsolidationModelMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.financial.sharing.consolidationReport.entity.TblConsolidationModel">
        <id column="MODEL_ID" property="modelId"/>
        <result column="MODEL_CODE" property="modelCode"/>
        <result column="MODEL_NAME" property="modelName"/>
        <result column="CONSOLIDATION_TYPE" property="consolidationType"/>
        <result column="PARENT_ORG_ID" property="parentOrgId"/>
        <result column="CONSOLIDATION_SCOPE" property="consolidationScope"/>
        <result column="PERIOD_TYPE" property="periodType"/>
        <result column="START_PERIOD" property="startPeriod"/>
        <result column="END_PERIOD" property="endPeriod"/>
        <result column="CURRENCY_CODE" property="currencyCode"/>
        <result column="EXCHANGE_RATE_TYPE" property="exchangeRateType"/>
        <result column="IS_AUTO_ELIMINATION" property="isAutoElimination"/>
        <result column="ELIMINATION_RULES" property="eliminationRules"/>
        <result column="STATUS" property="status"/>
        <result column="TENANT_ID" property="tenantId"/>
        <result column="CREATE_USER" property="createUser"/>
        <result column="CREATE_TIME" property="createTime"/>
        <result column="UPDATE_USER" property="updateUser"/>
        <result column="UPDATE_TIME" property="updateTime"/>
    </resultMap>

    <!-- 查询合并模型列表 -->
    <select id="selectModelList" resultMap="BaseResultMap">
        SELECT
            MODEL_ID,
            MODEL_CODE,
            MODEL_NAME,
            CONSOLIDATION_TYPE,
            PARENT_ORG_ID,
            CONSOLIDATION_SCOPE,
            PERIOD_TYPE,
            START_PERIOD,
            END_PERIOD,
            CURRENCY_CODE,
            EXCHANGE_RATE_TYPE,
            IS_AUTO_ELIMINATION,
            ELIMINATION_RULES,
            STATUS,
            TENANT_ID,
            CREATE_USER,
            CREATE_TIME,
            UPDATE_USER,
            UPDATE_TIME
        FROM TBL_CONSOLIDATION_MODEL
        WHERE 1=1
        <if test="param.modelCode != null and param.modelCode != ''">
            AND MODEL_CODE LIKE CONCAT('%', #{param.modelCode}, '%')
        </if>
        <if test="param.modelName != null and param.modelName != ''">
            AND MODEL_NAME LIKE CONCAT('%', #{param.modelName}, '%')
        </if>
        <if test="param.consolidationType != null and param.consolidationType != ''">
            AND CONSOLIDATION_TYPE = #{param.consolidationType}
        </if>
        <if test="param.parentOrgId != null and param.parentOrgId != ''">
            AND PARENT_ORG_ID = #{param.parentOrgId}
        </if>
        <if test="param.status != null and param.status != ''">
            AND STATUS = #{param.status}
        </if>
        ORDER BY CREATE_TIME DESC
    </select>

    <!-- 检查模型编码是否存在 -->
    <select id="checkModelCodeExists" resultType="int">
        SELECT COUNT(*)
        FROM TBL_CONSOLIDATION_MODEL
        WHERE MODEL_CODE = #{modelCode}
        AND TENANT_ID = #{tenantId}
        <if test="excludeModelId != null and excludeModelId != ''">
            AND MODEL_ID != #{excludeModelId}
        </if>
    </select>

</mapper>

