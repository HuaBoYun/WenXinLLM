<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.budgetPlanning.mapper.BudgetFormMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.financial.sharing.budgetPlanning.entity.TblBudgetForm">
        <id column="FORM_ID" property="formId"/>
        <result column="MODEL_ID" property="modelId"/>
        <result column="FORM_CODE" property="formCode"/>
        <result column="FORM_NAME" property="formName"/>
        <result column="FORM_TYPE" property="formType"/>
        <result column="FORM_CONFIG" property="formConfig"/>
        <result column="STATUS" property="status"/>
        <result column="DESCRIPTION" property="description"/>
        <result column="SORT_ORDER" property="sortOrder"/>
        <result column="ORG_ID" property="orgId"/>
        <result column="CREATE_USER" property="createUser"/>
        <result column="CREATE_TIME" property="createTime"/>
        <result column="UPDATE_USER" property="updateUser"/>
        <result column="UPDATE_TIME" property="updateTime"/>
        <result column="MODEL_NAME" property="modelName"/>
    </resultMap>

    <!-- 查询预算表单配置列表 -->
    <select id="selectFormList" resultMap="BaseResultMap">
        SELECT
            f.FORM_ID,
            f.MODEL_ID,
            f.FORM_CODE,
            f.FORM_NAME,
            f.FORM_TYPE,
            f.FORM_CONFIG,
            f.STATUS,
            f.DESCRIPTION,
            f.SORT_ORDER,
            f.ORG_ID,
            f.CREATE_USER,
            f.CREATE_TIME,
            f.UPDATE_USER,
            f.UPDATE_TIME,
            m.MODEL_NAME
        FROM TBL_BUDGET_FORM f
        LEFT JOIN TBL_BUDGET_MODEL m ON f.MODEL_ID = m.MODEL_ID
        <where>
            <if test="param.modelId != null and param.modelId != ''">
                AND f.MODEL_ID = #{param.modelId}
            </if>
            <if test="param.formCode != null and param.formCode != ''">
                AND f.FORM_CODE LIKE CONCAT('%', #{param.formCode}, '%')
            </if>
            <if test="param.formName != null and param.formName != ''">
                AND f.FORM_NAME LIKE CONCAT('%', #{param.formName}, '%')
            </if>
            <if test="param.formType != null and param.formType != ''">
                AND f.FORM_TYPE = #{param.formType}
            </if>
            <if test="param.status != null and param.status != ''">
                AND f.STATUS = #{param.status}
            </if>
        </where>
        ORDER BY f.SORT_ORDER, f.CREATE_TIME DESC
    </select>

    <!-- 根据表单编码查询预算表单配置 -->
    <select id="selectByFormCode" resultMap="BaseResultMap">
        SELECT
            f.FORM_ID,
            f.MODEL_ID,
            f.FORM_CODE,
            f.FORM_NAME,
            f.FORM_TYPE,
            f.FORM_CONFIG,
            f.STATUS,
            f.DESCRIPTION,
            f.SORT_ORDER,
            f.ORG_ID,
            f.CREATE_USER,
            f.CREATE_TIME,
            f.UPDATE_USER,
            f.UPDATE_TIME,
            m.MODEL_NAME
        FROM TBL_BUDGET_FORM f
        LEFT JOIN TBL_BUDGET_MODEL m ON f.MODEL_ID = m.MODEL_ID
        WHERE f.FORM_CODE = #{formCode}
    </select>

    <!-- 检查表单编码是否存在 -->
    <select id="checkFormCodeExists" resultType="int">
        SELECT COUNT(*)
        FROM TBL_BUDGET_FORM
        WHERE FORM_CODE = #{formCode}
        <if test="excludeId != null and excludeId != ''">
            AND FORM_ID != #{excludeId}
        </if>
    </select>

</mapper>

