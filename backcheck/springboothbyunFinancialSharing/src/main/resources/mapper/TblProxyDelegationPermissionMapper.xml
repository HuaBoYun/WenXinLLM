<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblProxyDelegationPermissionMapper">

    <select id="selectByDelegationId" resultType="com.financial.sharing.entity.TblProxyDelegationPermission">
        SELECT * FROM TBL_PROXY_DELEGATION_PERMISSION WHERE DELEGATION_ID = #{delegationId} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByPermissionType" resultType="com.financial.sharing.entity.TblProxyDelegationPermission">
        SELECT * FROM TBL_PROXY_DELEGATION_PERMISSION WHERE PERMISSION_TYPE = #{permissionType} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByPermissionValue" resultType="com.financial.sharing.entity.TblProxyDelegationPermission">
        SELECT * FROM TBL_PROXY_DELEGATION_PERMISSION WHERE PERMISSION_VALUE = #{permissionValue} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByDelegationIdAndType" resultType="com.financial.sharing.entity.TblProxyDelegationPermission">
        SELECT * FROM TBL_PROXY_DELEGATION_PERMISSION
        WHERE DELEGATION_ID = #{delegationId} AND PERMISSION_TYPE = #{permissionType}
        ORDER BY CREATE_TIME DESC
    </select>

</mapper>
