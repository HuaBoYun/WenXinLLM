<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblAuditRuleMapper">

    <select id="selectByRuleCode" resultType="com.financial.sharing.entity.TblAuditRule">
        SELECT * FROM TBL_AUDIT_RULE WHERE RULE_CODE = #{ruleCode}
    </select>

    <select id="selectByRuleType" resultType="com.financial.sharing.entity.TblAuditRule">
        SELECT * FROM TBL_AUDIT_RULE WHERE RULE_TYPE = #{ruleType} ORDER BY PRIORITY DESC, CREATE_TIME DESC
    </select>

    <select id="selectByIsEnabled" resultType="com.financial.sharing.entity.TblAuditRule">
        SELECT * FROM TBL_AUDIT_RULE WHERE IS_ENABLED = #{isEnabled} ORDER BY PRIORITY DESC, CREATE_TIME DESC
    </select>

    <select id="selectByRuleTypeAndEnabled" resultType="com.financial.sharing.entity.TblAuditRule">
        SELECT * FROM TBL_AUDIT_RULE
        WHERE RULE_TYPE = #{ruleType} AND IS_ENABLED = #{isEnabled}
        ORDER BY PRIORITY DESC, CREATE_TIME DESC
    </select>

    <select id="selectAllEnabledRules" resultType="com.financial.sharing.entity.TblAuditRule">
        SELECT * FROM TBL_AUDIT_RULE
        WHERE IS_ENABLED = 1
        ORDER BY PRIORITY DESC, CREATE_TIME DESC
    </select>

</mapper>
