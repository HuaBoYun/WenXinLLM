<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblBillConfigMapper">

    <select id="selectByConfigCode" resultType="com.financial.sharing.entity.TblBillConfig">
        SELECT * FROM TBL_BILL_CONFIG WHERE CONFIG_CODE = #{configCode}
    </select>

    <select id="selectByBillType" resultType="com.financial.sharing.entity.TblBillConfig">
        SELECT * FROM TBL_BILL_CONFIG WHERE BILL_TYPE = #{billType} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByOrgId" resultType="com.financial.sharing.entity.TblBillConfig">
        SELECT * FROM TBL_BILL_CONFIG WHERE ORG_ID = #{orgId} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByIsEnabled" resultType="com.financial.sharing.entity.TblBillConfig">
        SELECT * FROM TBL_BILL_CONFIG WHERE IS_ENABLED = #{isEnabled} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByBillTypeAndOrgId" resultType="com.financial.sharing.entity.TblBillConfig">
        SELECT * FROM TBL_BILL_CONFIG
        WHERE BILL_TYPE = #{billType} AND ORG_ID = #{orgId}
        ORDER BY CREATE_TIME DESC
    </select>

</mapper>
