<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblAuditRuleLogMapper">

    <select id="selectByRuleId" resultType="com.financial.sharing.entity.TblAuditRuleLog">
        SELECT * FROM TBL_AUDIT_RULE_LOG
        WHERE RULE_ID = #{ruleId}
        ORDER BY EXECUTION_TIME DESC
    </select>

    <select id="selectByBusiness" resultType="com.financial.sharing.entity.TblAuditRuleLog">
        SELECT * FROM TBL_AUDIT_RULE_LOG
        WHERE BUSINESS_TYPE = #{businessType} AND BUSINESS_ID = #{businessId}
        ORDER BY EXECUTION_TIME DESC
    </select>

    <select id="selectByExecutionResult" resultType="com.financial.sharing.entity.TblAuditRuleLog">
        SELECT * FROM TBL_AUDIT_RULE_LOG
        WHERE EXECUTION_RESULT = #{executionResult}
        ORDER BY EXECUTION_TIME DESC
    </select>

    <select id="selectByExecutionUser" resultType="com.financial.sharing.entity.TblAuditRuleLog">
        SELECT * FROM TBL_AUDIT_RULE_LOG
        WHERE EXECUTION_USER = #{executionUser}
        ORDER BY EXECUTION_TIME DESC
    </select>

    <select id="selectByRuleIdAndResult" resultType="com.financial.sharing.entity.TblAuditRuleLog">
        SELECT * FROM TBL_AUDIT_RULE_LOG
        WHERE RULE_ID = #{ruleId} AND EXECUTION_RESULT = #{executionResult}
        ORDER BY EXECUTION_TIME DESC
    </select>

    <select id="selectByBusinessTypeOrderByTime" resultType="com.financial.sharing.entity.TblAuditRuleLog">
        SELECT * FROM TBL_AUDIT_RULE_LOG
        WHERE BUSINESS_TYPE = #{businessType}
        ORDER BY EXECUTION_TIME DESC
    </select>

</mapper>
