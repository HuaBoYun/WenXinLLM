<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblProxyDelegationHistoryMapper">

    <select id="selectByDelegationId" resultType="com.financial.sharing.entity.TblProxyDelegationHistory">
        SELECT * FROM TBL_PROXY_DELEGATION_HISTORY WHERE DELEGATION_ID = #{delegationId} ORDER BY OPERATION_TIME DESC
    </select>

    <select id="selectByOperationType" resultType="com.financial.sharing.entity.TblProxyDelegationHistory">
        SELECT * FROM TBL_PROXY_DELEGATION_HISTORY WHERE OPERATION_TYPE = #{operationType} ORDER BY OPERATION_TIME DESC
    </select>

    <select id="selectByOperator" resultType="com.financial.sharing.entity.TblProxyDelegationHistory">
        SELECT * FROM TBL_PROXY_DELEGATION_HISTORY WHERE OPERATOR = #{operator} ORDER BY OPERATION_TIME DESC
    </select>

    <select id="selectByDelegationIdAndOperationType" resultType="com.financial.sharing.entity.TblProxyDelegationHistory">
        SELECT * FROM TBL_PROXY_DELEGATION_HISTORY
        WHERE DELEGATION_ID = #{delegationId} AND OPERATION_TYPE = #{operationType}
        ORDER BY OPERATION_TIME DESC
    </select>

</mapper>
