<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblTravelArchiveEvaluationMapper">

    <select id="selectByArchiveId" resultType="com.financial.sharing.entity.TblTravelArchiveEvaluation">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_EVALUATION WHERE ARCHIVE_ID = #{archiveId} ORDER BY EVALUATION_TIME DESC
    </select>

    <select id="selectByEvaluationUser" resultType="com.financial.sharing.entity.TblTravelArchiveEvaluation">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_EVALUATION WHERE EVALUATION_USER = #{evaluationUser} ORDER BY EVALUATION_TIME DESC
    </select>

    <select id="selectByOverallScoreRange" resultType="com.financial.sharing.entity.TblTravelArchiveEvaluation">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_EVALUATION
        WHERE OVERALL_SCORE BETWEEN #{minScore} AND #{maxScore}
        ORDER BY EVALUATION_TIME DESC
    </select>

    <select id="selectAvgOverallScoreByArchiveId" resultType="java.lang.Double">
        SELECT AVG(OVERALL_SCORE) FROM TBL_TRAVEL_ARCHIVE_EVALUATION WHERE ARCHIVE_ID = #{archiveId}
    </select>

    <select id="countByArchiveId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM TBL_TRAVEL_ARCHIVE_EVALUATION WHERE ARCHIVE_ID = #{archiveId}
    </select>

</mapper>
