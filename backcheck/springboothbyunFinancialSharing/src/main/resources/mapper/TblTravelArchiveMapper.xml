<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblTravelArchiveMapper">

    <select id="selectByArchiveCode" resultType="com.financial.sharing.entity.TblTravelArchive">
        SELECT * FROM TBL_TRAVEL_ARCHIVE WHERE ARCHIVE_CODE = #{archiveCode}
    </select>

    <select id="selectByArchiveType" resultType="com.financial.sharing.entity.TblTravelArchive">
        SELECT * FROM TBL_TRAVEL_ARCHIVE WHERE ARCHIVE_TYPE = #{archiveType} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByProviderCode" resultType="com.financial.sharing.entity.TblTravelArchive">
        SELECT * FROM TBL_TRAVEL_ARCHIVE WHERE PROVIDER_CODE = #{providerCode} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByProviderNameLike" resultType="com.financial.sharing.entity.TblTravelArchive">
        SELECT * FROM TBL_TRAVEL_ARCHIVE WHERE PROVIDER_NAME LIKE CONCAT('%', #{providerName}, '%') ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByStarLevel" resultType="com.financial.sharing.entity.TblTravelArchive">
        SELECT * FROM TBL_TRAVEL_ARCHIVE WHERE STAR_LEVEL = #{starLevel} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByIsEnabled" resultType="com.financial.sharing.entity.TblTravelArchive">
        SELECT * FROM TBL_TRAVEL_ARCHIVE WHERE IS_ENABLED = #{isEnabled} ORDER BY CREATE_TIME DESC
    </select>

</mapper>
