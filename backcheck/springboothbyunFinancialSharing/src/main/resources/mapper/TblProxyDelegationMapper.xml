<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblProxyDelegationMapper">

    <select id="selectByDelegationCode" resultType="com.financial.sharing.entity.TblProxyDelegation">
        SELECT * FROM TBL_PROXY_DELEGATION WHERE DELEGATION_CODE = #{delegationCode}
    </select>

    <select id="selectByDelegationType" resultType="com.financial.sharing.entity.TblProxyDelegation">
        SELECT * FROM TBL_PROXY_DELEGATION WHERE DELEGATION_TYPE = #{delegationType} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByDelegatorId" resultType="com.financial.sharing.entity.TblProxyDelegation">
        SELECT * FROM TBL_PROXY_DELEGATION WHERE DELEGATOR_ID = #{delegatorId} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByProxyId" resultType="com.financial.sharing.entity.TblProxyDelegation">
        SELECT * FROM TBL_PROXY_DELEGATION WHERE PROXY_ID = #{proxyId} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByIsEnabled" resultType="com.financial.sharing.entity.TblProxyDelegation">
        SELECT * FROM TBL_PROXY_DELEGATION WHERE IS_ENABLED = #{isEnabled} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectValidDelegationByDelegator" resultType="com.financial.sharing.entity.TblProxyDelegation">
        SELECT * FROM TBL_PROXY_DELEGATION
        WHERE DELEGATOR_ID = #{delegatorId}
        AND IS_ENABLED = 1
        AND START_DATE &lt;= #{currentDate}
        AND END_DATE &gt;= #{currentDate}
        ORDER BY CREATE_TIME DESC
    </select>

</mapper>
