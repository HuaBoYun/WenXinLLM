<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.sharing.mapper.TblTravelArchivePriceMapper">

    <select id="selectByArchiveId" resultType="com.financial.sharing.entity.TblTravelArchivePrice">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_PRICE WHERE ARCHIVE_ID = #{archiveId} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByCity" resultType="com.financial.sharing.entity.TblTravelArchivePrice">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_PRICE WHERE CITY = #{city} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByRoomType" resultType="com.financial.sharing.entity.TblTravelArchivePrice">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_PRICE WHERE ROOM_TYPE = #{roomType} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByVehicleType" resultType="com.financial.sharing.entity.TblTravelArchivePrice">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_PRICE WHERE VEHICLE_TYPE = #{vehicleType} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByIsEnabled" resultType="com.financial.sharing.entity.TblTravelArchivePrice">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_PRICE WHERE IS_ENABLED = #{isEnabled} ORDER BY CREATE_TIME DESC
    </select>

    <select id="selectByArchiveIdAndCity" resultType="com.financial.sharing.entity.TblTravelArchivePrice">
        SELECT * FROM TBL_TRAVEL_ARCHIVE_PRICE
        WHERE ARCHIVE_ID = #{archiveId} AND CITY = #{city}
        ORDER BY CREATE_TIME DESC
    </select>

</mapper>
