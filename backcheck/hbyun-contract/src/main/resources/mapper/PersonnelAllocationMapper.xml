<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.contract.mapper.PersonnelAllocationMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.huabo.contract.entity.PersonnelAllocation">
        <id column="id" property="id" />
        <result column="planning_id" property="planningId" />
        <result column="position_name" property="positionName" />
        <result column="position_type" property="positionType" />
        <result column="required_count" property="requiredCount" />
        <result column="allocated_count" property="allocatedCount" />
        <result column="skill_requirements" property="skillRequirements" />
        <result column="experience_requirements" property="experienceRequirements" />
        <result column="education_requirements" property="educationRequirements" />
        <result column="budgeted_cost" property="budgetedCost" />
        <result column="actual_cost" property="actualCost" />
        <result column="allocation_status" property="allocationStatus" />
        <result column="priority" property="priority" />
        <result column="start_date" property="startDate" />
        <result column="end_date" property="endDate" />
        <result column="manager_id" property="managerId" />
        <result column="manager_name" property="managerName" />
        <result column="remarks" property="remarks" />
        <result column="create_by" property="createBy" />
        <result column="create_time" property="createTime" />
        <result column="update_by" property="updateBy" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, planning_id, position_name, position_type, required_count, allocated_count,
        skill_requirements, experience_requirements, education_requirements, budgeted_cost,
        actual_cost, allocation_status, priority, start_date, end_date, manager_id,
        manager_name, remarks, create_by, create_time, update_by, update_time
    </sql>

    <!-- 分页查询人员配置列表 -->
    <select id="selectPersonnelAllocationPage" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM personnel_allocation
        WHERE 1=1
        <if test="param.planningId != null">
            AND planning_id = #{param.planningId}
        </if>
        <if test="param.positionName != null and param.positionName != ''">
            AND position_name LIKE CONCAT('%', #{param.positionName}, '%')
        </if>
        <if test="param.positionType != null">
            AND position_type = #{param.positionType}
        </if>
        <if test="param.allocationStatus != null">
            AND allocation_status = #{param.allocationStatus}
        </if>
        <if test="param.priority != null">
            AND priority = #{param.priority}
        </if>
        <if test="param.managerId != null">
            AND manager_id = #{param.managerId}
        </if>
        <if test="param.managerName != null and param.managerName != ''">
            AND manager_name LIKE CONCAT('%', #{param.managerName}, '%')
        </if>
        <if test="param.minBudgetedCost != null">
            AND budgeted_cost >= #{param.minBudgetedCost}
        </if>
        <if test="param.maxBudgetedCost != null">
            AND budgeted_cost &lt;= #{param.maxBudgetedCost}
        </if>
        <if test="param.minActualCost != null">
            AND actual_cost >= #{param.minActualCost}
        </if>
        <if test="param.maxActualCost != null">
            AND actual_cost &lt;= #{param.maxActualCost}
        </if>
        <if test="param.startDate != null">
            AND start_date >= #{param.startDate}
        </if>
        <if test="param.endDate != null">
            AND end_date &lt;= #{param.endDate}
        </if>
        <if test="param.createStartTime != null">
            AND create_time >= #{param.createStartTime}
        </if>
        <if test="param.createEndTime != null">
            AND create_time &lt;= #{param.createEndTime}
        </if>
        <if test="param.createBy != null">
            AND create_by = #{param.createBy}
        </if>
        <if test="param.keyword != null and param.keyword != ''">
            AND (position_name LIKE CONCAT('%', #{param.keyword}, '%')
                 OR skill_requirements LIKE CONCAT('%', #{param.keyword}, '%')
                 OR manager_name LIKE CONCAT('%', #{param.keyword}, '%')
                 OR remarks LIKE CONCAT('%', #{param.keyword}, '%'))
        </if>
        ORDER BY create_time DESC, id DESC
    </select>

    <!-- 根据项目策划ID查询人员配置列表 -->
    <select id="selectByPlanningId" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM personnel_allocation
        WHERE planning_id = #{planningId}
        ORDER BY priority DESC, create_time DESC
    </select>

    <!-- 根据配置状态查询人员配置列表 -->
    <select id="selectByAllocationStatus" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM personnel_allocation
        WHERE allocation_status = #{allocationStatus}
        ORDER BY create_time DESC
    </select>

    <!-- 根据岗位类型查询人员配置列表 -->
    <select id="selectByPositionType" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM personnel_allocation
        WHERE position_type = #{positionType}
        ORDER BY create_time DESC
    </select>

    <!-- 根据优先级查询人员配置列表 -->
    <select id="selectByPriority" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM personnel_allocation
        WHERE priority = #{priority}
        ORDER BY create_time DESC
    </select>

    <!-- 查询负责人的人员配置列表 -->
    <select id="selectByManagerId" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM personnel_allocation
        WHERE manager_id = #{managerId}
        ORDER BY create_time DESC
    </select>

    <!-- 根据项目策划ID统计预算成本总额 -->
    <select id="sumBudgetedCostByPlanning" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(budgeted_cost), 0)
        FROM personnel_allocation
        WHERE planning_id = #{planningId}
    </select>

    <!-- 根据项目策划ID统计实际成本总额 -->
    <select id="sumActualCostByPlanning" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(actual_cost), 0)
        FROM personnel_allocation
        WHERE planning_id = #{planningId}
    </select>

    <!-- 根据岗位类型统计需求人数 -->
    <select id="sumRequiredCountByPositionType" resultType="java.lang.Integer">
        SELECT COALESCE(SUM(required_count), 0)
        FROM personnel_allocation
        WHERE position_type = #{positionType}
    </select>

    <!-- 根据岗位类型统计已分配人数 -->
    <select id="sumAllocatedCountByPositionType" resultType="java.lang.Integer">
        SELECT COALESCE(SUM(allocated_count), 0)
        FROM personnel_allocation
        WHERE position_type = #{positionType}
    </select>

    <!-- 根据配置状态统计数量 -->
    <select id="countByAllocationStatus" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM personnel_allocation
        WHERE allocation_status = #{allocationStatus}
    </select>

    <!-- 查询超预算的人员配置列表 -->
    <select id="selectOverBudget" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM personnel_allocation
        WHERE actual_cost > budgeted_cost
        ORDER BY (actual_cost - budgeted_cost) DESC
    </select>

    <!-- 查询配置不足的人员配置列表 -->
    <select id="selectUnderAllocated" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM personnel_allocation
        WHERE allocated_count &lt; required_count
        ORDER BY (required_count - allocated_count) DESC
    </select>

    <!-- 查询高优先级的人员配置列表 -->
    <select id="selectHighPriority" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM personnel_allocation
        WHERE priority >= 4
        ORDER BY priority DESC, create_time DESC
    </select>

    <!-- 查询紧急的人员配置列表 -->
    <select id="selectUrgent" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM personnel_allocation
        WHERE allocation_status = 1
        AND start_date BETWEEN SYSDATE AND SYSDATE + 7
        ORDER BY start_date ASC
    </select>

    <!-- 批量更新配置状态 -->
    <update id="batchUpdateAllocationStatus">
        UPDATE personnel_allocation
        SET allocation_status = #{allocationStatus},
            update_time = SYSDATE
        <if test="updateBy != null">
            , update_by = #{updateBy}
        </if>
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 批量更新实际成本 -->
    <update id="batchUpdateActualCost">
        UPDATE personnel_allocation
        SET actual_cost = #{actualCost},
            update_time = SYSDATE
        <if test="updateBy != null">
            , update_by = #{updateBy}
        </if>
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
