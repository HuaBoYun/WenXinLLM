<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.legal.exam.modules.paper.mapper.PaperMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.huabo.legal.exam.modules.paper.entity.Paper">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="depart_id" property="departId" />
        <result column="exam_id" property="examId" />
        <result column="title" property="title" />
        <result column="total_time" property="totalTime" />
        <result column="user_time" property="userTime" />
        <result column="total_score" property="totalScore" />
        <result column="qualify_score" property="qualifyScore" />
        <result column="obj_score" property="objScore" />
        <result column="subj_score" property="subjScore" />
        <result column="user_score" property="userScore" />
        <result column="has_saq" property="hasSaq" />
        <result column="state" property="state" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="limit_time" property="limitTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        `id`,`userid`,`departid`,`examid`,`title`,`totaltime`,`usertime`,`totalscore`,`qualifyscore`,`objscore`,`subjscore`,`userscore`,`hassaq`,`state`,`createtime`,`updatetime`,`limittime`
    </sql>


    <resultMap id="ListResultMap"
               extends="BaseResultMap"
               type="com.huabo.legal.exam.modules.paper.dto.response.PaperListRespDTO">
        <result column="realname" property="realName" />
    </resultMap>

    <select id="paging" resultType="com.huabo.legal.exam.modules.paper.dto.response.PaperListRespDTO">
        SELECT pp.*,uc.realname FROM tbl_fwgl_paper pp
        LEFT JOIN tbl_staff uc ON pp.userid=uc.staffid
        <where>
            <if test="query!=null">
                <if test="query.examId!=null and query.examId!=''">
                    AND pp.examid=#{query.examId}
                </if>

                <if test="query.userId!=null and query.userId!=''">
                    AND pp.userid=#{query.userId}
                </if>

                <if test="query.departId!=null and query.departId!=''">
                    AND pp.departid=#{query.departId}
                </if>

                <if test="query.state!=null">
                    AND pp.state=#{query.state}
                </if>
                <if test="query.realName!=null and query.realName!=''">
                    AND uc.realname  LIKE '%${query.realName}%'
                </if>
            </if>
        </where>

        ORDER BY pp.createtime DESC
    </select>

    <update id="updateExamLeaveJob">
        update tbl_fwgl_paper set state = 3 where id=#{id}
    </update>

</mapper>
