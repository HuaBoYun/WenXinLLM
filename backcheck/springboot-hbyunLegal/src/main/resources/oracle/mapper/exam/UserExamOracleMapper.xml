<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.legal.exam.modules.user.exam.mapper.UserExamMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.huabo.legal.exam.modules.user.exam.entity.UserExam">
        <id column="id" property="id" />
        <result column="userid" property="userId" />
        <result column="examid" property="examId" />
        <result column="trycount" property="tryCount" />
        <result column="maxscore" property="maxScore" />
        <result column="passed" property="passed" />
        <result column="createtime" property="createTime" />
        <result column="updatetime" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        `id`,`userid`,`examid`,`trycount`,`maxscore`,`passed`,`createtime`,`updatetime`
    </sql>


    <resultMap id="ListResultMap"
               type="com.huabo.legal.exam.modules.user.exam.dto.response.UserExamRespDTO"
               extends="BaseResultMap">
        <result column="title" property="title" />
        <result column="realname" property="realName" />
    </resultMap>


    <select id="paging" resultMap="ListResultMap">
        SELECT ue.*,ee.title,uc.realname FROM tbl_fwgl_user_exam ue
        LEFT JOIN tbl_fwgl_exam ee ON ue.examid=ee.id
        LEFT JOIN tbl_staff uc ON ue.userid=uc.staffid
        WHERE ee.id IS NOT NULL AND uc.staffid IS NOT NULL

        <if test="query!=null">

            <if test="query.userId!=null and query.userId!=''">
                AND ue.userid='{{userId}}'
            </if>

            <if test="query.examId!=null and query.examId!=''">
                AND ue.examid = #{query.examId}
            </if>

            <if test="query.title!=null and query.title!=''">
                AND ee.title LIKE '%${query.title}%'
            </if>
            <if test="query.realName!=null and query.realName!=''">
                AND uc.realname LIKE '%${query.realName}%'
            </if>
        </if>

    </select>
</mapper>
