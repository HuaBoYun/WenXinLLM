<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.legal.oracle.mapper.TblStaffOracleMapper">

    <select id="findBelongGroupIdUserInfo" resultType="com.huabo.legal.vo.result.StaffResult">
        select ORGNAME as belongGroupName from TBL_ORGANIZATION where ORGID=#{orgid}
    </select>

    <select id="findWorkUnitIdUserInfo" resultType="com.huabo.legal.vo.result.StaffResult">
        select ORGNAME as workUnitName from TBL_ORGANIZATION where ORGID=#{orgid}
    </select>

    <select id="findUserInfo" resultType="com.huabo.legal.vo.result.StaffResult">
        select REALNAME as realName from TBL_STAFF where STAFFID=#{staffId}
    </select>

    <select id="findCreatorUserInfo" resultType="com.huabo.legal.vo.result.StaffResult">
        select REALNAME as realName from TBL_STAFF where STAFFID=#{staffId}
    </select>

    <select id="findCreatorUserInfos" resultType="com.huabo.legal.vo.result.UserInfo">
        select STAFFID as staffId,REALNAME as realName from TBL_STAFF where STAFFID in (${staffId})
    </select>

    <select id="findWorkUnitIdUserInfos" resultType="com.huabo.legal.vo.result.UserInfo">
        select ORGID as workUnitId,ORGNAME as workUnitName from TBL_ORGANIZATION where ORGID in (${orgid})
    </select>

    <select id="findBelongGroupIdUserInfos" resultType="com.huabo.legal.vo.result.UserInfo">
        select ORGID as belongGroupId,ORGNAME as belongGroupName from TBL_ORGANIZATION where ORGID in (${orgid})
    </select>

    <select id="findBelongGroupIdUserInfoOrgmeno" resultType="com.huabo.legal.vo.result.UserInfo">
        select ORGID as belongGroupId,orgmeno as belongGroupName from TBL_ORGANIZATION where ORGID in (${orgid})
    </select>

    <select id="findUserInfoExam" resultType="com.huabo.legal.vo.result.StaffResult">
        select * from TBL_STAFF where ROLEIDSTRS like '%${rid}%' and STAFFID=#{staffId}
    </select>

    <select id="findLikeUser" resultType="java.lang.Integer">
        SELECT RID from TBL_ROLE where rname = #{type} and COMPANYID =#{orgid}
    </select>

    <select id="isHeadquartersLegal" resultType="java.lang.Integer">
        SELECT count(*) from TBL_ROLE where COMPANYID in (SELECT ORGID from TBL_ORGANIZATION WHERE orgtype=2 or orgtype=1) AND RNAME= #{param.role}
    </select>
    
      <select id="getUserInfoForId" resultType="com.huabo.legal.oracle.entity.TblStaffOracle">
        SELECT * from TBL_STAFF where  STAFFID=#{staffId}
    </select>

    <select id="findAllList" resultType="com.huabo.legal.oracle.entity.TblOrganization">
        select * from TBL_ORGANIZATION where ORGTYPE &lt; 100 and ORGTYPE!=0
    </select>

    <select id="findTblOrganizationFatherorgidList" resultType="java.lang.Long">
        select ORGID from TBL_ORGANIZATION where ORGTYPE &lt; 100 and ORGTYPE!=0 and FATHERORGID =#{orgid}
    </select>
    

</mapper>
