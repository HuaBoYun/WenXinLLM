<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.huabo.system.mapper.RiskEventHandlingMapper">

    <!-- 查询风险事件处理统计 -->
    <select id="queryRiskEventHandling" resultType="java.util.Map">
        SELECT
            COUNT(CASE WHEN LOSSEVENTCATEGORY = 1 THEN 1 END) AS CATEGORY1,
            COUNT(CASE WHEN LOSSEVENTCATEGORY = 2 THEN 1 END) AS CATEGORY2
        FROM TBL_RISKEVENT
    </select>

</mapper>

