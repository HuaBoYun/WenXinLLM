<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.system.mapper.TblSystemRefopmReminderV1Mapper">

    <select id="findBusinessList" resultType="com.huabo.system.vo.result.RefReminderListResult">
        select ${reminderBusinessId} as businessId,${reminderBusinessStaff} as staffId,${reminderBusinessTime} as businessTime
        <if test="reminderBusinessContent != null and reminderBusinessContent != ''">
            ,${reminderBusinessContent} as content
        </if>
        from ${reminderBusinessTable}
    </select>

</mapper>