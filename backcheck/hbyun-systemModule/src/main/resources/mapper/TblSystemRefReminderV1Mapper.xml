<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.system.mapper.TblSystemRefReminderV1Mapper">

    <select id="getList" resultType="com.huabo.system.entity.TblSystemRefReminderV">
        select t1.*,t2.TYPENAME
        from TBL_SYSTEM_REF_REMINDER_V t1
        left join TBL_SYSTEM_REFOPM_REMINDER_V t2 on t2.ID = t1.REFOPMID
        where t1.REMINDERSTAFFID = #{param.creator}
        <if test="param.isRead != null ">
            and t1.ISREAD = #{param.isRead}
        </if>
        order by t1.ID desc
    </select>

</mapper>