<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.huabo.system.mapper.AuditIssueCountMapper">

    <!-- 查询审计问题数量统计 -->
    <select id="queryAuditIssueCount" resultType="java.util.Map">
        SELECT
            COUNT(1) AS C,
            TO_CHAR(STARTDATE, 'YYYY-MM') AS D
        FROM TBL_NBSJ_PROJECT
        WHERE
            STARTDATE &gt;= ADD_MONTHS(SYSDATE, -12)
            AND STARTDATE &lt; SYSDATE
        GROUP BY
            TO_CHAR(STARTDATE, 'YYYY-MM')
        ORDER BY
            TO_CHAR(STARTDATE, 'YYYY-MM')
    </select>

</mapper>

