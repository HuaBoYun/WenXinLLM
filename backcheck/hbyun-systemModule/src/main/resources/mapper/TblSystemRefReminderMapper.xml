<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.system.mapper.TblSystemRefReminderMapper">

    <select id="getList" resultType="com.huabo.system.entity.TblSystemRefReminder">
        select t1.*
        from TBL_SYSTEM_REF_REMINDER t1
        inner join TBL_SYSTEM_REFOPM_REMINDER t2 on t2.ID = t1.REFOPMID
        where t1.REMINDERSTAFFID = #{param.creator} and t2.MODULEROUTE = #{param.moduleRoute}
        order by t1.ID desc
    </select>

</mapper>