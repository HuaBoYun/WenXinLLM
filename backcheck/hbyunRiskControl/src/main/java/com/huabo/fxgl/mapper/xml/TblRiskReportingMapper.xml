<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.fxgl.mapper.TblRiskReportingMapper">

	<select id="selectAllList" resultType="com.huabo.fxgl.entity.TblRiskReportingEntity">
		select r.*,o.orgname as FillDeptName,g.orgname as
		fillUnitName,s.REALNAME from TBL_RISK_REPORTING r
		left join TBL_ORGANIZATION o on r.FILL_DEPTID=o.ORGID
		left join TBL_ORGANIZATION g on r.FILL_UNIT=g.ORGID
		left join TBL_STAFF s on r.FILL_STAFFID=s.STAFFID where 1=1
		<if test="queryParam.entName!=null and queryParam.entName!='' ">
			and ent_Name  LIKE concat(concat('%',#{queryParam.entName} ),'%')
		</if>
		order by id desc
	</select>
	
	<select id="exportRiskReport" resultType="com.huabo.fxgl.entity.TblRiskReportingEntity">
		select r.*,o.orgname as FillDeptName,g.orgname as
		fillUnitName,s.REALNAME from TBL_RISK_REPORTING r
		left join TBL_ORGANIZATION o on r.FILL_DEPTID=o.ORGID
		left join TBL_ORGANIZATION g on r.FILL_UNIT=g.ORGID
		left join TBL_STAFF s on r.FILL_STAFFID=s.STAFFID where 1=1
		<if test="entname!=null and entname!='' ">
			and ent_Name  LIKE concat(concat('%',#{entname} ),'%')
		</if>
		 <if test="ids!=null and ids.length>0">
	        and id in 
	     <foreach collection="ids" item="item" open="(" close=")" separator=",">
	      #{item}	   
	     </foreach>
	     </if>
		order by id desc
	</select>
	
	
</mapper>
