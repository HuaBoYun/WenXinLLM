<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.fxgl.mapper.StaffMapper">
     
     <select id="getOrgNameByIds" resultType="com.huabo.fxgl.entity.Staff" >
 select realname from tbl_staff where 1=1  
  <if test="ids!=null and ids.length>0">
	    and staffid IN 
	    <foreach collection="ids" open="(" close=")" item="item" separator=",">
	     #{item}
	    </foreach>
	   </if>
</select>
     
     <select id="getOrgIdByNames" resultType="com.huabo.fxgl.entity.Staff" >
 select * from tbl_staff where 1=1  
  <if test="ids!=null and ids.length>0">
	    and realname IN 
	    <foreach collection="ids" open="(" close=")" item="item" separator=",">
	     #{item}
	    </foreach>
	   </if>
	   <if test="orgid!=null">
	     and orgid in (SELECT orgid FROM tbl_organization START WITH FATHERORGID = #{orgid} CONNECT BY PRIOR FATHERORGID=orgid)
	   </if>
</select>


  <select id="getOrgIdByNames2" resultType="com.huabo.fxgl.entity.Staff" >
   select * from tbl_staff where 1=1  
     <if test="ids!=null and ids.length>0">
	    and realname IN 
	    <foreach collection="ids" open="(" close=")" item="item" separator=",">
	     #{item}
	    </foreach>
	   </if>
	   
	      <if test="orgids!=null and orgids.size()>0">
	    and orgid IN 
	    <foreach collection="orgids" open="(" close=")" item="item" separator=",">
	     #{item}
	    </foreach>
	   </if>
</select>
</mapper>
