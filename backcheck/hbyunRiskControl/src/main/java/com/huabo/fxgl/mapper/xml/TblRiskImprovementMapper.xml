<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huabo.fxgl.mapper.TblRiskImprovementMapper">

	<select id="getOrgGroupList" resultType="java.util.Map">
		select BRANCH_ID from TBL_RISK_IMPROVEMENT_DETAILS where 1=1  
		<if test="name!=null and name!='' ">
		and BRANCH_NAME   LIKE concat(concat('%',#{name} ),'%')
		</if>
		 group by BRANCH_ID  
	</select>
	
</mapper>
