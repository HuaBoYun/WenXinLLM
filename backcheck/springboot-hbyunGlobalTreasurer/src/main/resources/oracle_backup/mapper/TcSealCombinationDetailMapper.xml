<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.global.treasurer.oracle.mapper.TcSealCombinationDetailMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.global.treasurer.oracle.entity.TcSealCombinationDetail">
        <id column="ID" jdbcType="VARCHAR" property="id" />
        <result column="COMBINATION_ID" jdbcType="VARCHAR" property="combinationId" />
        <result column="SEAL_ID" jdbcType="VARCHAR" property="sealId" />
        <result column="IS_REQUIRED" jdbcType="VARCHAR" property="isRequired" />
        <result column="USAGE_ORDER" jdbcType="INTEGER" property="usageOrder" />
        <result column="ALTERNATIVE_SEAL_ID" jdbcType="VARCHAR" property="alternativeSealId" />
        <result column="USAGE_CONDITION" jdbcType="VARCHAR" property="usageCondition" />
        <result column="STATUS" jdbcType="VARCHAR" property="status" />
        <result column="REMARK" jdbcType="VARCHAR" property="remark" />
        <result column="CREATE_TIME" jdbcType="TIMESTAMP" property="createTime" />
        <result column="UPDATE_TIME" jdbcType="TIMESTAMP" property="updateTime" />
        <result column="CREATE_USER" jdbcType="VARCHAR" property="createUser" />
        <result column="UPDATE_USER" jdbcType="VARCHAR" property="updateUser" />
        <result column="VERSION_NO" jdbcType="INTEGER" property="versionNo" />
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        ID, COMBINATION_ID, SEAL_ID, IS_REQUIRED, USAGE_ORDER, ALTERNATIVE_SEAL_ID,
        USAGE_CONDITION, STATUS, REMARK, CREATE_TIME, UPDATE_TIME, CREATE_USER,
        UPDATE_USER, VERSION_NO
    </sql>

    <!-- 根据组合ID查询明细列表 -->
    <select id="selectByCombinationId" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM TC_SEAL_COMBINATION_DETAIL
        WHERE COMBINATION_ID = #{combinationId,jdbcType=VARCHAR}
        ORDER BY USAGE_ORDER ASC
    </select>

    <!-- 根据印鉴ID查询明细列表 -->
    <select id="selectBySealId" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM TC_SEAL_COMBINATION_DETAIL
        WHERE SEAL_ID = #{sealId,jdbcType=VARCHAR}
        ORDER BY CREATE_TIME DESC
    </select>

    <!-- 根据组合ID查询必选印鉴 -->
    <select id="selectRequiredByCombinationId" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM TC_SEAL_COMBINATION_DETAIL
        WHERE COMBINATION_ID = #{combinationId,jdbcType=VARCHAR}
        AND IS_REQUIRED = '1'
        AND STATUS = '1'
        ORDER BY USAGE_ORDER ASC
    </select>

    <!-- 根据组合ID查询可选印鉴 -->
    <select id="selectOptionalByCombinationId" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM TC_SEAL_COMBINATION_DETAIL
        WHERE COMBINATION_ID = #{combinationId,jdbcType=VARCHAR}
        AND IS_REQUIRED = '0'
        AND STATUS = '1'
        ORDER BY USAGE_ORDER ASC
    </select>

    <!-- 根据组合ID按使用顺序查询 -->
    <select id="selectByCombinationIdOrderByUsageOrder" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM TC_SEAL_COMBINATION_DETAIL
        WHERE COMBINATION_ID = #{combinationId,jdbcType=VARCHAR}
        AND STATUS = '1'
        ORDER BY USAGE_ORDER ASC, CREATE_TIME ASC
    </select>

    <!-- 根据组合ID删除明细 -->
    <delete id="deleteByCombinationId" parameterType="java.lang.String">
        DELETE FROM TC_SEAL_COMBINATION_DETAIL
        WHERE COMBINATION_ID = #{combinationId,jdbcType=VARCHAR}
    </delete>

    <!-- 批量插入组合明细 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO TC_SEAL_COMBINATION_DETAIL (
            ID, COMBINATION_ID, SEAL_ID, IS_REQUIRED, USAGE_ORDER,
            ALTERNATIVE_SEAL_ID, USAGE_CONDITION, STATUS, REMARK,
            CREATE_TIME, CREATE_USER, VERSION_NO
        ) VALUES
        <foreach collection="details" item="detail" separator=",">
            (
                #{detail.id,jdbcType=VARCHAR},
                #{detail.combinationId,jdbcType=VARCHAR},
                #{detail.sealId,jdbcType=VARCHAR},
                #{detail.isRequired,jdbcType=VARCHAR},
                #{detail.usageOrder,jdbcType=INTEGER},
                #{detail.alternativeSealId,jdbcType=VARCHAR},
                #{detail.usageCondition,jdbcType=VARCHAR},
                #{detail.status,jdbcType=VARCHAR},
                #{detail.remark,jdbcType=VARCHAR},
                #{detail.createTime,jdbcType=TIMESTAMP},
                #{detail.createUser,jdbcType=VARCHAR},
                #{detail.versionNo,jdbcType=INTEGER}
            )
        </foreach>
    </insert>

    <!-- 统计组合中印鉴数量 -->
    <select id="countByCombinationId" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM TC_SEAL_COMBINATION_DETAIL
        WHERE COMBINATION_ID = #{combinationId,jdbcType=VARCHAR}
        AND STATUS = '1'
    </select>

    <!-- 检查印鉴是否在组合中使用 -->
    <select id="countBySealId" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM TC_SEAL_COMBINATION_DETAIL
        WHERE SEAL_ID = #{sealId,jdbcType=VARCHAR}
        AND STATUS = '1'
    </select>

</mapper>