<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.global.treasurer.oracle.mapper.TcSecurityParameterMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.global.treasurer.entity.TcSecurityParameter">
        <id column="PARAMID" property="paramId" jdbcType="BIGINT"/>
        <result column="PARAMCODE" property="paramCode" jdbcType="VARCHAR"/>
        <result column="PARAMNAME" property="paramName" jdbcType="VARCHAR"/>
        <result column="PARAMVALUE" property="paramValue" jdbcType="VARCHAR"/>
        <result column="PARAMTYPE" property="paramType" jdbcType="VARCHAR"/>
        <result column="DESCRIPTION" property="description" jdbcType="VARCHAR"/>
        <result column="ISENABLED" property="isEnabled" jdbcType="INTEGER"/>
        <result column="ISENCRYPTED" property="isEncrypted" jdbcType="INTEGER"/>
        <result column="ORGID" property="orgId" jdbcType="BIGINT"/>
        <result column="CREATETIME" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="CREATEUSER" property="createUser" jdbcType="BIGINT"/>
        <result column="UPDATETIME" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="UPDATEUSER" property="updateUser" jdbcType="BIGINT"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        PARAMID, PARAMCODE, PARAMNAME, PARAMVALUE, PARAMTYPE, DESCRIPTION,
        ISENABLED, ISENCRYPTED, ORGID, CREATETIME, CREATEUSER, UPDATETIME, UPDATEUSER
    </sql>

    <!-- 根据主键查询 -->
    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMID = #{id}
    </select>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMID = #{paramId}
    </select>

    <!-- 根据条件查询 -->
    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_TREASURY_PARAMETERS
        WHERE 1=1
        <if test="paramCode != null and paramCode != ''">
            AND PARAMCODE LIKE '%' || #{paramCode} || '%'
        </if>
        <if test="paramName != null and paramName != ''">
            AND PARAMNAME LIKE '%' || #{paramName} || '%'
        </if>
        <if test="paramType != null and paramType != ''">
            AND PARAMTYPE = #{paramType}
        </if>
        <if test="isEnabled != null and isEnabled != ''">
            AND ISENABLED = #{isEnabled}
        </if>
        ORDER BY CREATETIME DESC
    </select>

    <!-- 新增 -->
    <insert id="insert" parameterType="com.global.treasurer.entity.TcSecurityParameter" useGeneratedKeys="true" keyProperty="paramId">
        INSERT INTO TBL_TREASURY_PARAMETERS (
            PARAMCODE, PARAMNAME, PARAMVALUE, PARAMTYPE, DESCRIPTION,
            ISENABLED, ISENCRYPTED, ORGID, CREATETIME, CREATEUSER,
            UPDATETIME, UPDATEUSER
        ) VALUES (
            #{paramCode}, #{paramName}, #{paramValue}, #{paramType}, #{description},
            #{isEnabled}, #{isEncrypted}, #{orgId}, #{createTime}, #{createUser},
            #{updateTime}, #{updateUser}
        )
    </insert>

    <!-- 根据主键更新 -->
    <update id="updateByPrimaryKey" parameterType="com.global.treasurer.entity.TcSecurityParameter">
        UPDATE TBL_TREASURY_PARAMETERS
        SET PARAMCODE = #{paramCode},
            PARAMNAME = #{paramName},
            PARAMVALUE = #{paramValue},
            PARAMTYPE = #{paramType},
            DESCRIPTION = #{description},
            ISENABLED = #{isEnabled},
            ISENCRYPTED = #{isEncrypted},
            UPDATETIME = #{updateTime},
            UPDATEUSER = #{updateUser}
        WHERE PARAMID = #{paramId}
    </update>

    <!-- 根据ID更新 -->
    <update id="updateById" parameterType="com.global.treasurer.entity.TcSecurityParameter">
        UPDATE TBL_TREASURY_PARAMETERS
        SET PARAMCODE = #{paramCode},
            PARAMNAME = #{paramName},
            PARAMVALUE = #{paramValue},
            PARAMTYPE = #{paramType},
            DESCRIPTION = #{description},
            ISENABLED = #{isEnabled},
            ISENCRYPTED = #{isEncrypted},
            UPDATETIME = #{updateTime},
            UPDATEUSER = #{updateUser}
        WHERE PARAMID = #{paramId}
    </update>

    <!-- 根据主键删除 -->
    <delete id="deleteByPrimaryKey">
        DELETE FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMID = #{id}
    </delete>

    <!-- 根据ID删除 -->
    <delete id="deleteById">
        DELETE FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMID = #{paramId}
    </delete>

    <!-- 批量删除 -->
    <delete id="batchDelete">
        DELETE FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMID IN
        <foreach collection="paramIds" item="paramId" open="(" separator="," close=")">
            #{paramId}
        </foreach>
    </delete>

    <!-- 更新状态 -->
    <update id="updateStatus">
        UPDATE TBL_TREASURY_PARAMETERS
        SET ISENABLED = #{params.isEnabled},
            UPDATETIME = #{params.updateTime},
            UPDATEUSER = #{params.updateUser}
        WHERE PARAMID = #{params.paramId}
    </update>

    <!-- 查询总数 -->
    <select id="selectTotalCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM TBL_TREASURY_PARAMETERS
    </select>

    <!-- 根据参数类型查询 -->
    <select id="selectByParamType" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMTYPE = #{paramType}
        ORDER BY PARAMCODE
    </select>

    <!-- 根据状态查询 -->
    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_TREASURY_PARAMETERS
        WHERE ISENABLED = #{isEnabled}
        ORDER BY PARAMCODE
    </select>

    <!-- 根据组织ID查询 -->
    <select id="selectByOrgId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_TREASURY_PARAMETERS
        WHERE ORGID = #{orgId}
        ORDER BY PARAMCODE
    </select>

    <!-- 检查参数编码是否存在 -->
    <select id="checkParamCodeExists" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMCODE = #{paramCode} AND ORGID = #{orgId}
        <if test="paramId != null">
            AND PARAMID != #{paramId}
        </if>
    </select>

    <!-- 根据参数编码查询 -->
    <select id="selectByParamCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMCODE = #{paramCode} AND ORGID = #{orgId}
    </select>

    <!-- 查询启用的系统参数 -->
    <select id="selectEnabledSystemParams" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMTYPE = 'SYSTEM' AND ISENABLED = 1
        <if test="orgId != null">
            AND ORGID = #{orgId}
        </if>
        ORDER BY PARAMCODE
    </select>

    <!-- 查询启用的业务参数 -->
    <select id="selectEnabledBusinessParams" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMTYPE = 'BUSINESS' AND ISENABLED = 1
        <if test="orgId != null">
            AND ORGID = #{orgId}
        </if>
        ORDER BY PARAMCODE
    </select>

    <!-- 查询启用的安全参数 -->
    <select id="selectEnabledSecurityParams" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMTYPE = 'SECURITY' AND ISENABLED = 1
        <if test="orgId != null">
            AND ORGID = #{orgId}
        </if>
        ORDER BY PARAMCODE
    </select>

    <!-- 查询启用的接口参数 -->
    <select id="selectEnabledInterfaceParams" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMTYPE = 'INTERFACE' AND ISENABLED = 1
        <if test="orgId != null">
            AND ORGID = #{orgId}
        </if>
        ORDER BY PARAMCODE
    </select>

    <!-- 获取所有参数类型 -->
    <select id="selectAllParameterTypes" resultType="java.util.Map">
        SELECT DISTINCT PARAMTYPE as value, PARAMTYPE as label
        FROM TBL_TREASURY_PARAMETERS
        WHERE PARAMTYPE IS NOT NULL
        ORDER BY PARAMTYPE
    </select>

    <!-- 统计总数 -->
    <select id="countTotal" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM TBL_TREASURY_PARAMETERS
    </select>

    <!-- 根据启用状态统计数量 -->
    <select id="countByIsEnabled" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM TBL_TREASURY_PARAMETERS
        WHERE ISENABLED = #{isEnabled}
    </select>

    <!-- 按参数类型统计数量 -->
    <select id="countByParamType" resultType="java.util.Map">
        SELECT PARAMTYPE as paramType, COUNT(*) as count
        FROM TBL_TREASURY_PARAMETERS
        GROUP BY PARAMTYPE
        ORDER BY PARAMTYPE
    </select>

    <!-- 统计今日修改数量 -->
    <select id="countTodayUpdate" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM TBL_TREASURY_PARAMETERS
        WHERE DATE(UPDATETIME) = CURRENT_DATE
    </select>

    <!-- 批量更新状态 -->
    <update id="batchUpdateStatus">
        UPDATE TBL_TREASURY_PARAMETERS
        SET ISENABLED = #{isEnabled},
            UPDATETIME = #{updateTime},
            UPDATEUSER = #{updateUser}
        WHERE PARAMID IN
        <foreach collection="paramIds" item="paramId" open="(" separator="," close=")">
            #{paramId}
        </foreach>
    </update>

</mapper>