<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.global.treasurer.mapper.BondInvestmentMapper">

    <!-- 结果映射 -->
    <resultMap id="BondInvestmentResult" type="com.global.treasurer.entity.TblBondInvestment">
        <id property="investmentId" column="INVESTMENT_ID"/>
        <result property="investmentNo" column="INVESTMENT_NO"/>
        <result property="planId" column="PLAN_ID"/>
        <result property="accountId" column="ACCOUNT_ID"/>
        <result property="bondCode" column="BOND_CODE"/>
        <result property="bondName" column="BOND_NAME"/>
        <result property="bondType" column="BOND_TYPE"/>
        <result property="issuer" column="ISSUER"/>
        <result property="faceValue" column="FACE_VALUE"/>
        <result property="couponRate" column="COUPON_RATE"/>
        <result property="purchasePrice" column="PURCHASE_PRICE"/>
        <result property="purchaseQuantity" column="PURCHASE_QUANTITY"/>
        <result property="purchaseAmount" column="PURCHASE_AMOUNT"/>
        <result property="purchaseDate" column="PURCHASE_DATE"/>
        <result property="maturityDate" column="MATURITY_DATE"/>
        <result property="currentPrice" column="CURRENT_PRICE"/>
        <result property="currentValue" column="CURRENT_VALUE"/>
        <result property="yieldToMaturity" column="YIELD_TO_MATURITY"/>
        <result property="accruedInterest" column="ACCRUED_INTEREST"/>
        <result property="unrealizedGainLoss" column="UNREALIZED_GAIN_LOSS"/>
        <result property="investmentStatus" column="INVESTMENT_STATUS"/>
        <result property="createTime" column="CREATE_TIME"/>
        <result property="updateTime" column="UPDATE_TIME"/>
        <result property="createUser" column="CREATE_USER"/>
        <result property="updateUser" column="UPDATE_USER"/>
    </resultMap>

    <!-- 根据查询条件查询债券投资列表 -->
    <select id="selectByQueryDTO" parameterType="com.global.treasurer.dto.BondInvestmentQueryDTO"
            resultMap="BondInvestmentResult">
        SELECT * FROM TBL_BOND_INVESTMENT
        WHERE DELETE_FLAG = 0
        <if test="investmentNo != null and investmentNo != ''">
            AND INVESTMENT_NO LIKE CONCAT('%', #{investmentNo}, '%')
        </if>
        <if test="bondCode != null and bondCode != ''">
            AND BOND_CODE LIKE CONCAT('%', #{bondCode}, '%')
        </if>
        <if test="bondType != null and bondType != ''">
            AND BOND_TYPE = #{bondType}
        </if>
        <if test="investmentStatus != null and investmentStatus != ''">
            AND INVESTMENT_STATUS = #{investmentStatus}
        </if>
        <if test="startDate != null">
            AND PURCHASE_DATE &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            AND PURCHASE_DATE &lt;= #{endDate}
        </if>
        ORDER BY CREATED_TIME DESC
    </select>

    <!-- 根据投资编号查询 -->
    <select id="selectByInvestmentNo" parameterType="String" resultMap="BondInvestmentResult">
        SELECT * FROM TBL_BOND_INVESTMENT
        WHERE INVESTMENT_NO = #{investmentNo}
    </select>

</mapper>
