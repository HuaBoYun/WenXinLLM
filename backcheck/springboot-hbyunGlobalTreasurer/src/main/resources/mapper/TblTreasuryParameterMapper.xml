<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.global.treasurer.mapper.TblTreasuryParameterMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.global.treasurer.entity.TblTreasuryParameter">
        <id column="ID" property="id" />
        <result column="PARAM_CODE" property="paramCode" />
        <result column="PARAM_NAME" property="paramName" />
        <result column="PARAM_TYPE" property="paramType" />
        <result column="PARAM_VALUE" property="paramValue" />
        <result column="DESCRIPTION" property="description" />
        <result column="IS_ENABLED" property="isEnabled" />
        <result column="CREATE_BY" property="createBy" />
        <result column="CREATE_TIME" property="createTime" />
        <result column="UPDATE_BY" property="updateBy" />
        <result column="UPDATE_TIME" property="updateTime" />
        <result column="REMARK" property="remark" />
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        ID, PARAM_CODE, PARAM_NAME, PARAM_TYPE, PARAM_VALUE, DESCRIPTION,
        IS_ENABLED, CREATE_BY, CREATE_TIME, UPDATE_BY, UPDATE_TIME, REMARK
    </sql>

    <!-- selectList - 查询列表,配合PageHelper使用分页 -->
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM TBL_TREASURY_PARAMETER
        <if test="ew != null and ew.sqlSegment != null and ew.sqlSegment != ''">
            ${ew.sqlSegment}
        </if>
    </select>

    <!-- selectById -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM TBL_TREASURY_PARAMETER
        WHERE ID = #{id}
    </select>

    <!-- selectOne -->
    <select id="selectOne" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM TBL_TREASURY_PARAMETER
        <if test="ew != null and ew.sqlSegment != null and ew.sqlSegment != ''">
            ${ew.sqlSegment}
        </if>
        LIMIT 1
    </select>

    <!-- selectCount -->
    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM TBL_TREASURY_PARAMETER
        <if test="ew != null and ew.sqlSegment != null and ew.sqlSegment != ''">
            ${ew.sqlSegment}
        </if>
    </select>

    <!-- insert -->
    <insert id="insert" parameterType="com.global.treasurer.entity.TblTreasuryParameter">
        INSERT INTO TBL_TREASURY_PARAMETER (
            ID, PARAM_CODE, PARAM_NAME, PARAM_TYPE, PARAM_VALUE, DESCRIPTION,
            IS_ENABLED, CREATE_BY, CREATE_TIME, UPDATE_BY, UPDATE_TIME, REMARK
        ) VALUES (
            #{id}, #{paramCode}, #{paramName}, #{paramType}, #{paramValue}, #{description},
            #{isEnabled}, #{createBy}, #{createTime}, #{updateBy}, #{updateTime}, #{remark}
        )
    </insert>

    <!-- updateById -->
    <update id="updateById" parameterType="com.global.treasurer.entity.TblTreasuryParameter">
        UPDATE TBL_TREASURY_PARAMETER
        <set>
            <if test="paramCode != null">PARAM_CODE = #{paramCode},</if>
            <if test="paramName != null">PARAM_NAME = #{paramName},</if>
            <if test="paramType != null">PARAM_TYPE = #{paramType},</if>
            <if test="paramValue != null">PARAM_VALUE = #{paramValue},</if>
            <if test="description != null">DESCRIPTION = #{description},</if>
            <if test="isEnabled != null">IS_ENABLED = #{isEnabled},</if>
            <if test="createBy != null">CREATE_BY = #{createBy},</if>
            <if test="createTime != null">CREATE_TIME = #{createTime},</if>
            <if test="updateBy != null">UPDATE_BY = #{updateBy},</if>
            <if test="updateTime != null">UPDATE_TIME = #{updateTime},</if>
            <if test="remark != null">REMARK = #{remark}</if>
        </set>
        WHERE ID = #{id}
    </update>

    <!-- deleteById -->
    <delete id="deleteById">
        DELETE FROM TBL_TREASURY_PARAMETER WHERE ID = #{id}
    </delete>

</mapper>
