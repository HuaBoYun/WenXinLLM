<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.global.treasurer.mapper.ReportTemplateMapper">

    <resultMap id="BaseResultMap" type="com.global.treasurer.entity.TblReportTemplate">
        <id column="TEMPLATE_ID" property="templateId"/>
        <result column="TEMPLATE_CODE" property="templateCode"/>
        <result column="TEMPLATE_NAME" property="templateName"/>
        <result column="AUTHORITY_ID" property="authorityId"/>
        <result column="TEMPLATE_TYPE" property="templateType"/>
        <result column="TEMPLATE_VERSION" property="templateVersion"/>
        <result column="TEMPLATE_CONTENT" property="templateContent"/>
        <result column="TEMPLATE_FILE_PATH" property="templateFilePath"/>
        <result column="EFFECTIVE_DATE" property="effectiveDate"/>
        <result column="EXPIRY_DATE" property="expiryDate"/>
        <result column="IS_ENABLED" property="isEnabled"/>
        <result column="REPORT_FREQUENCY" property="reportFrequency"/>
        <result column="DESCRIPTION" property="description"/>
        <result column="COMPANY_ID" property="companyId"/>
        <result column="DELETE_FLAG" property="deleteFlag"/>
        <result column="CREATED_BY" property="createdBy"/>
        <result column="CREATED_TIME" property="createdTime"/>
        <result column="UPDATED_BY" property="updatedBy"/>
        <result column="UPDATED_TIME" property="updatedTime"/>
        <result column="REMARK" property="remark"/>
    </resultMap>

    <sql id="Base_Column_List">
        TEMPLATE_ID, TEMPLATE_CODE, TEMPLATE_NAME, AUTHORITY_ID, TEMPLATE_TYPE, TEMPLATE_VERSION,
        TEMPLATE_CONTENT, TEMPLATE_FILE_PATH, EFFECTIVE_DATE, EXPIRY_DATE, IS_ENABLED, REPORT_FREQUENCY,
        DESCRIPTION, COMPANY_ID, DELETE_FLAG, CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, REMARK
    </sql>

    <select id="selectTemplateList" resultMap="BaseResultMap" parameterType="map">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_REPORT_TEMPLATE
        WHERE DELETE_FLAG = 0
        <if test="templateCode != null and templateCode != ''">
            AND TEMPLATE_CODE LIKE CONCAT('%', #{templateCode}, '%')
        </if>
        <if test="templateName != null and templateName != ''">
            AND TEMPLATE_NAME LIKE CONCAT('%', #{templateName}, '%')
        </if>
        <if test="authorityId != null and authorityId != ''">
            AND AUTHORITY_ID = #{authorityId}
        </if>
        <if test="templateType != null and templateType != ''">
            AND TEMPLATE_TYPE = #{templateType}
        </if>
        <if test="isEnabled != null">
            AND IS_ENABLED = #{isEnabled}
        </if>
        <if test="companyId != null and companyId != ''">
            AND COMPANY_ID = #{companyId}
        </if>
        ORDER BY CREATED_TIME DESC
    </select>

    <select id="selectTemplateById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_REPORT_TEMPLATE
        WHERE TEMPLATE_ID = #{templateId} AND DELETE_FLAG = 0
    </select>

    <select id="selectByTemplateCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_REPORT_TEMPLATE
        WHERE TEMPLATE_CODE = #{templateCode} AND DELETE_FLAG = 0
    </select>

    <select id="selectByAuthorityId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_REPORT_TEMPLATE
        WHERE AUTHORITY_ID = #{authorityId} AND DELETE_FLAG = 0 AND IS_ENABLED = 1
        ORDER BY CREATED_TIME DESC
    </select>

    <select id="selectUsableTemplates" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_REPORT_TEMPLATE
        WHERE DELETE_FLAG = 0 AND IS_ENABLED = 1
          AND (EFFECTIVE_DATE IS NULL OR EFFECTIVE_DATE &lt;= SYSDATE)
          AND (EXPIRY_DATE IS NULL OR EXPIRY_DATE &gt;= SYSDATE)
        ORDER BY CREATED_TIME DESC
    </select>

    <select id="selectExpiringSoonTemplates" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_REPORT_TEMPLATE
        WHERE DELETE_FLAG = 0 AND IS_ENABLED = 1
          AND EXPIRY_DATE BETWEEN SYSDATE AND SYSDATE + #{days}
        ORDER BY EXPIRY_DATE ASC
    </select>

    <update id="batchDeleteByIds">
        UPDATE TBL_REPORT_TEMPLATE
        SET DELETE_FLAG = 1, UPDATED_TIME = SYSDATE
        WHERE TEMPLATE_ID IN
        <foreach collection="templateIds" item="templateId" open="(" separator="," close=")">
            #{templateId}
        </foreach>
    </update>

    <update id="updateTemplateStatus">
        UPDATE TBL_REPORT_TEMPLATE
        SET IS_ENABLED = #{isEnabled}, UPDATED_TIME = SYSDATE
        WHERE TEMPLATE_ID = #{templateId}
    </update>

</mapper>

