<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.global.treasurer.mapper.CreditLimitMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.global.treasurer.entity.TblCreditLimit">
        <id column="LIMIT_ID" property="limitId"/>
        <result column="LIMIT_NO" property="limitNo"/>
        <result column="CONTRACT_ID" property="contractId"/>
        <result column="COMPANY_ID" property="companyId"/>
        <result column="COMPANY_NAME" property="companyName"/>
        <result column="LIMIT_TYPE" property="limitType"/>
        <result column="TOTAL_LIMIT" property="totalLimit"/>
        <result column="USED_LIMIT" property="usedLimit"/>
        <result column="FROZEN_LIMIT" property="frozenLimit"/>
        <result column="AVAILABLE_LIMIT" property="availableLimit"/>
        <result column="CURRENCY_CODE" property="currencyCode"/>
        <result column="START_DATE" property="startDate"/>
        <result column="END_DATE" property="endDate"/>
        <result column="LIMIT_STATUS" property="limitStatus"/>
        <result column="INTEREST_RATE" property="interestRate"/>
        <result column="GUARANTEE_METHOD" property="guaranteeMethod"/>
        <result column="PURPOSE" property="purpose"/>
        <result column="DELETE_FLAG" property="deleteFlag"/>
        <result column="CREATED_TIME" property="createdTime"/>
        <result column="UPDATED_TIME" property="updatedTime"/>
        <result column="REMARK" property="remark"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        LIMIT_ID, LIMIT_NO, CONTRACT_ID, COMPANY_ID, COMPANY_NAME, LIMIT_TYPE,
        TOTAL_LIMIT, USED_LIMIT, FROZEN_LIMIT, AVAILABLE_LIMIT, CURRENCY_CODE,
        START_DATE, END_DATE, LIMIT_STATUS, INTEREST_RATE, GUARANTEE_METHOD,
        PURPOSE, DELETE_FLAG, CREATED_TIME, UPDATED_TIME, REMARK
    </sql>

    <!-- 根据ID查询授信额度详情 -->
    <select id="selectLimitById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_CREDIT_LIMIT
        WHERE LIMIT_ID = #{limitId}
        AND (DELETE_FLAG = 0 OR DELETE_FLAG IS NULL)
    </select>

    <!-- 分页查询授信额度列表 -->
    <select id="selectLimitList" parameterType="map" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_CREDIT_LIMIT
        WHERE (DELETE_FLAG = 0 OR DELETE_FLAG IS NULL)
        <if test="limitNo != null and limitNo != ''">
            AND LIMIT_NO LIKE CONCAT('%', #{limitNo}, '%')
        </if>
        <if test="companyId != null">
            AND COMPANY_ID = #{companyId}
        </if>
        <if test="companyName != null and companyName != ''">
            AND COMPANY_NAME LIKE CONCAT('%', #{companyName}, '%')
        </if>
        <if test="limitType != null and limitType != ''">
            AND LIMIT_TYPE = #{limitType}
        </if>
        <if test="limitStatus != null and limitStatus != ''">
            AND LIMIT_STATUS = #{limitStatus}
        </if>
        <if test="contractId != null">
            AND CONTRACT_ID = #{contractId}
        </if>
        ORDER BY CREATED_TIME DESC
    </select>

    <!-- 根据合同ID查询额度列表 -->
    <select id="selectByContractId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM TBL_CREDIT_LIMIT
        WHERE CONTRACT_ID = #{contractId}
        AND (DELETE_FLAG = 0 OR DELETE_FLAG IS NULL)
        ORDER BY CREATED_TIME DESC
    </select>

    <!-- 更新额度状态 -->
    <update id="updateLimitStatus">
        UPDATE TBL_CREDIT_LIMIT
        SET LIMIT_STATUS = #{status},
            UPDATED_TIME = CURRENT_TIMESTAMP
        WHERE LIMIT_ID = #{limitId}
    </update>

    <!-- 更新已用额度 -->
    <update id="updateUsedLimit">
        UPDATE TBL_CREDIT_LIMIT
        SET USED_LIMIT = #{usedLimit},
            AVAILABLE_LIMIT = TOTAL_LIMIT - #{usedLimit} - FROZEN_LIMIT,
            UPDATED_TIME = CURRENT_TIMESTAMP
        WHERE LIMIT_ID = #{limitId}
    </update>

    <!-- 冻结额度 -->
    <update id="freezeLimit">
        UPDATE TBL_CREDIT_LIMIT
        SET FROZEN_LIMIT = FROZEN_LIMIT + #{frozenAmount},
            AVAILABLE_LIMIT = AVAILABLE_LIMIT - #{frozenAmount},
            UPDATED_TIME = CURRENT_TIMESTAMP
        WHERE LIMIT_ID = #{limitId}
    </update>

    <!-- 解冻额度 -->
    <update id="unfreezeLimit">
        UPDATE TBL_CREDIT_LIMIT
        SET FROZEN_LIMIT = FROZEN_LIMIT - #{unfreezeAmount},
            AVAILABLE_LIMIT = AVAILABLE_LIMIT + #{unfreezeAmount},
            UPDATED_TIME = CURRENT_TIMESTAMP
        WHERE LIMIT_ID = #{limitId}
    </update>

    <!-- 批量删除授信额度（逻辑删除） -->
    <update id="batchDeleteByIds">
        UPDATE TBL_CREDIT_LIMIT
        SET DELETE_FLAG = 1,
            UPDATED_TIME = CURRENT_TIMESTAMP
        WHERE LIMIT_ID IN
        <foreach collection="limitIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 查询授信额度汇总 -->
    <select id="selectLimitSummary" parameterType="java.lang.Long" resultType="map">
        SELECT 
            COUNT(*) as totalCount,
            SUM(TOTAL_LIMIT) as totalLimit,
            SUM(USED_LIMIT) as usedLimit,
            SUM(FROZEN_LIMIT) as frozenLimit,
            SUM(AVAILABLE_LIMIT) as availableLimit
        FROM TBL_CREDIT_LIMIT
        WHERE (DELETE_FLAG = 0 OR DELETE_FLAG IS NULL)
        <if test="companyId != null">
            AND COMPANY_ID = #{companyId}
        </if>
    </select>

</mapper>

