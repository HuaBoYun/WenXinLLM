<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.global.treasurer.financialProductDefinition.mapper.RiskAlertMapper">

    <!-- 按状态统计预警数量 -->
    <select id="countByStatus" resultType="java.util.Map">
        SELECT
            ALERT_STATUS as status,
            COUNT(*) as count
        FROM REDACTED.TBL_RISK_ALERT
        GROUP BY ALERT_STATUS
    </select>

    <!-- 按预警类型统计 -->
    <select id="countByType" resultType="java.util.Map">
        SELECT
            ALERT_TYPE as alertType,
            COUNT(*) as count
        FROM REDACTED.TBL_RISK_ALERT
        GROUP BY ALERT_TYPE
    </select>

    <!-- 按预警级别统计 -->
    <select id="countByLevel" resultType="java.util.Map">
        SELECT
            ALERT_LEVEL as alertLevel,
            COUNT(*) as count
        FROM REDACTED.TBL_RISK_ALERT
        GROUP BY ALERT_LEVEL
    </select>

</mapper>
