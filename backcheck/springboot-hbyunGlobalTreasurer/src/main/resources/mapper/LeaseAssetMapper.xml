<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.global.treasurer.mapper.LeaseAssetMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.global.treasurer.entity.TblLeaseAsset">
        <id property="assetId" column="ASSET_ID"/>
        <result property="leaseId" column="LEASE_ID"/>
        <result property="assetCode" column="ASSET_CODE"/>
        <result property="assetName" column="ASSET_NAME"/>
        <result property="assetType" column="ASSET_TYPE"/>
        <result property="assetValue" column="ASSET_VALUE"/>
        <result property="purchaseDate" column="PURCHASE_DATE"/>
        <result property="location" column="LOCATION"/>
        <result property="description" column="DESCRIPTION"/>
        <result property="status" column="STATUS"/>
        <result property="createdBy" column="CREATED_BY"/>
        <result property="createdTime" column="CREATED_TIME"/>
        <result property="updatedBy" column="UPDATED_BY"/>
        <result property="updatedTime" column="UPDATED_TIME"/>
    </resultMap>

    <!-- 根据租赁ID查询资产列表 -->
    <select id="selectByLeaseId" parameterType="long" resultMap="BaseResultMap">
        SELECT * FROM TBL_LEASE_ASSET
        WHERE LEASE_ID = #{leaseId}
        ORDER BY CREATED_TIME ASC
    </select>

    <!-- 根据资产ID查询资产详情 -->
    <select id="selectByAssetId" parameterType="long" resultMap="BaseResultMap">
        SELECT * FROM TBL_LEASE_ASSET
        WHERE ASSET_ID = #{assetId}
    </select>

    <!-- 批量删除资产 -->
    <delete id="batchDeleteByIds" parameterType="list">
        DELETE FROM TBL_LEASE_ASSET
        WHERE ASSET_ID IN
        <foreach collection="assetIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 根据租赁ID删除所有资产 -->
    <delete id="deleteByLeaseId" parameterType="long">
        DELETE FROM TBL_LEASE_ASSET
        WHERE LEASE_ID = #{leaseId}
    </delete>

    <!-- 统计租赁资产总值 -->
    <select id="sumAssetValueByLeaseId" parameterType="long" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(ASSET_VALUE), 0)
        FROM TBL_LEASE_ASSET
        WHERE LEASE_ID = #{leaseId}
    </select>

</mapper>

